# PHASE 3 — STEP 3E · CATEGORIES & BRANDS EXPERIENCE

**النوع:** CSS فقط (بلا JS · بلا Backend) · **التاريخ:** 2026-09-18
**أدوات:** `frontend/tools/qa/stage3e/` (`probe3e.py before|after` · `smoke3e.py` · `extra3e.py`) · JSON: `probe3e-before.json` · `probe3e-after.json` · `extra3e-before.json` · `extra3e-after.json`
**نسخ احتياطية:** `backup-shop.css` + `backup-md5-before.txt`

---

## 1) FILES MODIFIED

| الملف | md5 قبل → بعد |
|---|---|
| `frontend/shop/assets/css/shop.css` | `8e2e202a…` → `25634c90…` (**كتلة 3E مضافة فقط**، 8 قواعد + media) |
| `frontend/shop/assets/css/shop.filters.css` | `bb772219…` → **بلا تغيير** ✓ |
| **JS** | `shop.js 897310d7…` · `shop.pages.js 3b38eeee…` — **بلا تغيير** (0) |
| **Backend / DB / Routes / tokens.css / shop.home.css / shop.header.css / shop.search.css** | **0** ✓ |

`find frontend/shop frontend/assets src -newermt "18:20"` ⇒ **`shop.css` فقط** ✓ · الأقواس متوازنة (545/545) ✓ · **`!important` = 5 كما كان (0 جديد)** ✓

---

## 2) SOURCE / DOM MAP (متحقَّق من الكود)

| العنصر | الموقع |
|---|---|
| Renderer للتصنيفات والعلامات | **`taxonomy(view, kind)`** — `shop.pages.js:601-637` (**دالة واحدة مشتركة للاثنين**) |
| نقطة الاستدعاء | `products(view, ctx)`: `if (query.view === 'categories') return taxonomy(view,'categories')` (سطر 345-346) |
| Markup البطاقة (فهرس) | `div.shop-taxonomy > a.shop-taxonomy__item [ div > (span.shop-taxonomy__name + div.shop-note?) , span.shop-taxonomy__count ]` (635-643) |
| Selectors CSS | `shop.css`: `.shop-taxonomy` (456) · `.shop-taxonomy__item` (462) · hover/focus (476) · `__name` (483) · `__count` (488) · تجاوزات (1898-1936) · **scroller أفقي ≤640 (1909-1940)** · 3F (2463-2469) |
| المصدر الآخر | `shop.filters.css:32` — `.shop-taxonomy__icon` (**Home فقط** كما وثّق 3F) |
| النقر/التنقل | البطاقة كلها `<a>` واحدة → `#/products?categoryId=<id>` / `?brandId=<id>` (سطر 623) — **لم يتغيّر** ✓ |
| مصدر العدّاد | `item.productsCount` من **نفس** ردّ `GET /categories?limit=48` أو `/brands?limit=48` — **بلا أي طلب إضافي** ✓ (مثبت في §28) |
| التركيز | `.shop-taxonomy__item:focus-visible` (3F: حدّ أزرق) + `.shop-body :focus-visible` (3F: outline أزرق) |
| التحميل/الخطأ | `shop.loadingBlock` / `errorBlock` المشتركان — بلا تغيير ✓ |

## 3) ROUTE BEHAVIOR (قبل = بعد)

`#/products?view=categories` → فهرس التصنيفات · `#/products?view=brands` → فهرس العلامات · النقر → `#/products?categoryId=292|293` أو `?brandId=254` · لا مسارات جديدة ✓ · **بلا تغيير في الصيغة أو السلوك** ✓

## 4) SHARED SELECTOR USAGE MAP (إثبات عدم المساس بـHome)

| Selector | Home | Products | Categories index | Brands index |
|---|---|---|---|---|
| `.shop-taxonomy` | ✅ **داخل `section#shop-home-categories`** | ✗ | ✅ **ابن مباشر لـ`#shop-view`** | ✅ نفسه |
| `.shop-taxonomy__item` | ✅ (فيه `.shop-taxonomy__icon`) | ✗ | ✅ (بلا أيقونة) | ✅ |
| `.shop-taxonomy__icon` | ✅ فقط | ✗ | ✗ | ✗ |

**قواعد Home كلها ID-scoped** (`#shop-home-categories .shop-taxonomy…` في `shop.home.css:208-268, 444`) ⇒ أعلى تخصيصًا.
**ومع ذلك، لم أعتمد على التخصيص وحده:** كل قواعد 3E مقيّدة بـ**`#shop-view > .shop-taxonomy`** (ابن مباشر لـ`#shop-view`) — وبطاقات Home **ليست** أبناء مباشرين (داخل section) ⇒ **لا يمكن أن تتأثر** ✓ (مثبت بالقياس: §32 مطابق حرفيًا).
كذلك `.shop-section__head:has(+ .shop-taxonomy)` يطابق رأس الفهرس فقط (في صفحة المنتجات يتبع الرأس `.shop-filters`) ✓

## 5) CATEGORIES — المشاكل المرصودة قبل 3E

1. **جوال ≤640:** `.shop-taxonomy` كان **flex بتمرير أفقي** (`overflow-x:auto` + `min-width:58vw`) ⇒ البطاقة الثانية عند **x=−70** (390) و**x=−104** (600) ⇒ **مقصوصة/غير مرئية** بلا مؤشر تمرير (شريط مخفي).
2. **ديسكتوب:** `repeat(auto-fill, minmax(180px,1fr))` ⇒ **5 أعمدة × 189px** (1024) و**6 × 185px** (1280+) ⇒ عنقود صغير في أقصى اليمين داخل شبكة 1168px بأربعة أعمدة فارغة.
3. **ارتفاعات غير موحّدة:** 79px (جوال) · 82px (تصنيفات) · **64px (علامات)**.
4. **اسم طويل:** بلا أي حدّ ⇒ بطاقة **151px** (360) و**226px** (1366) واسم بارتفاع 197px (يكسر الشبكة).

## 6) BRANDS — المشاكل قبل 3E

نفس مشاكل التصنيفات + **ارتفاع 64px فقط** (سطر واحد) ⇒ بطاقات أقصر من التصنيفات (عدم اتساق بصري) ✓ وبنفس العنقود في الزاوية ✓.

## 7) MOBILE LAYOUT (360/390/430/600)

| العرض | قبل | بعد |
|---|---|---|
| الشبكة | flex + تمرير أفقي (لا أعمدة) | **عمود واحد بعرض المحتوى كاملًا** (336/366/406/576) |
| بطاقة | 219/226/249/348 × **79** · الثانية عند **x=−76/−70/−77/−104** | **336/366/406/576 × 92** · كلتاهما **x=12** (مرصوصة) |
| قصّ/تمرير أفقي | ✗ **مقصوصة** | ✅ **لا قصّ · لا تمرير أفقي** |
| فائض | 0 | **0** |

## 8) TABLET (641/768/769/900)

| العرض | قبل | بعد |
|---|---|---|
| 768 | 3 × 237 × 82/64 | **2 × 360 × 92** (بعرض كامل ✓ مقروء) |
| 769 | 3 × 238 | **2 × 276 × 96** |
| 900 | 4 × 208 | **3 × 276 × 96** |

## 9) DESKTOP (1024/1280/1366/1440/1920)

| العرض | قبل | بعد |
|---|---|---|
| 1024 | **5 × 189 × 82** | **3 × 276 × 96** |
| 1280-1920 | **6 × 185 × 82/64** | **4 × 276 × 96** |
| التوزيع | عنقود في أقصى اليمين + 4 أعمدة فارغة | **أعمدة ثابتة 276px تبدأ من بداية السطر (RTL)** + `justify-content: start` — بلا تمدد وبلا بطاقات ضخمة |

## 10) FEW-ITEM BEHAVIOR (بندان فعليًا)

- بندان ⇒ بطاقتان بعرض 276px عند **بداية السطر (يمين)** وبقية المساحة فارغة **بشكل مقصود** (لا تمدد إلى نصف الصفحة ولا عنقود مصغّر) ✓
- بند واحد ⇒ بطاقة واحدة بنفس العرض عند البداية (بلا توسيط مصطنع) ✓ · 3 بنود ⇒ صف كامل · بنود كثيرة ⇒ أسطر إضافية ✓ (السلوك من CSS Grid ✓)
- **لا تكرار ولا بيانات وهمية** ✓

## 11) LONG-NAME BEHAVIOR (اختبار داخل جلسة الفحص فقط — لا تعديل بيانات)

| الحالة | قبل | بعد |
|---|---|---|
| تصنيف طويل (360) | بطاقة **151px** · الاسم 101px | **92px** · الاسم **45px (سطران)** |
| تصنيف طويل (1024) | **202px** | **96px** |
| تصنيف طويل (1366) | **226px** (اسم 197px) | **96px** |
| علامة طويلة (390) | 103px (اسم 53px) | **92px** (اسم 45px) |
| علامة طويلة (1366) | **130px** (اسم 101px) | **96px** |
السبب: `-webkit-line-clamp: 2` + `overflow-wrap: anywhere` + `min-width: 0` ⇒ **الاسم سطران كحد أقصى** ولا يكسر البطاقة/الشبكة ✓ · فائض 0 في كل الحالات ✓

## 12) ZERO-COUNT BEHAVIOR

«تصنيف تجريبي — أجهزة منزلية» بعدّاد **«0 منتج»** يبقى **ظاهرًا** في الفهرس ✓ (لا إخفاء صامت ✓) بلون muted ✓ والتنقل يعمل ✓ (185×82 → **276×96** ✓). لم يُغيّر منطق الأعمال ✓

## 13) CARD ANATOMY

`a.shop-taxonomy__item` (رابط واحد — الهدف كامل البطاقة كما كان) → `div` (الاسم + الوصف إن وُجد) + `span.shop-taxonomy__count` — **بلا أيقونات/صور/أوصاف مُختلقة** ✓ (لم أُضف شيئًا) ✓

## 14) CARD DIMENSIONS

| | قبل | بعد |
|---|---|---|
| جوال | 219-348 × 79 | **336-576 × 92** |
| 768 | 237 × 82/64 | **360 × 92** |
| ديسكتوب | 185-238 × 82/64 | **276 × 96** |
الحشوة 16px (كما كانت) · نصف القطر 14px (`--radius-lg` كما كان) ✓ · `min-height` 92 (جوال) / 96 (≥769) — **ضمن 88-110 جوال و80-110 ديسكتوب** ✓ · الاسم 15px/600 · سطر 22.5 · العدّاد 12px muted ✓

## 15) GRID DIMENSIONS

عرض الشبكة = عرض المحتوى (336→1168 حسب العرض) · الفجوة **12px جوال / 16px ≥641** (كانت 8/12) · **لا `overflow-x` ولا snap** ✓ · الأعمدة: 1 / 2 / 3 / 4 بحسب المساحة ✓

## 16) INTRO COMPOSITION

- المسار + العنوان (19px/700) + الوصف: **نصوص كما هي** ✓ (لا نسخ تسويقي/إحصاءات) ✓
- **المسافة مقدمة→شبكة: 12→24px (جوال) و16→24px (ديسكتوب)** ✓ (إيقاع مقصود)
- عرض الوصف محدود بـ`62ch` للقراءة ✓ · المحاذاة كما هي (بداية RTL) ✓ · نفس حاوية صفحة المنتجات (1200/pad 16) ✓

## 17) LIGHT MODE

سطح البطاقة `--store-surface` #ffffff · حدّ `--store-border` #e6ecf3 · الاسم `--store-text` **17.85** · العدّاد `--store-text-muted` **4.76** ✓ — **توكنز 3F كما هي بلا أي لون جديد** ✓

## 18) DARK MODE

سطح #121b2b · حدّ #1e2a3d · الاسم **15.75** · العدّاد **6.73** ✓ · البطاقات متمايزة عن الخلفية (#0a1220) بالحدّ والسطح ✓ · **لا جزر بيضاء** ✓ · لم تُفتَّح أكثر من اللازم ✓

## 19) HOVER (ديسكتوب)

حدّ **أزرق المتجر** + `--shadow-sm` + رفع **2px** — **كما كانت في 3F بالضبط** (لم أزد الارتفاع/الحركة) ✓ · لا اعتماد على hover لفهم القابلية للنقر (البطاقة رابط كامل + مؤشر ✓)

## 20) FOCUS

`outline: 2px solid rgb(31,95,191)` + `box-shadow: none` + حدّ أزرق ✓ — **نظام أزرق واحد بلا teal وبلا حلقة مزدوجة** ✓ · **لم أغيّر `.shop-body :focus-visible` العام** (تغييره كان في 3F ✓) ✓

## 21) RTL

`justify-content: start` (بداية RTL = يمين) · خصائص منطقية فقط (`padding-inline`, `margin-inline`) · لا `left/right` جديدة ✓ · المسار/العنوان/البطاقات تبدأ من اليمين ✓ · الأسماء الطويلة لا تخرق الحافة ✓

## 22) LOADING

`loadingBlock` المشترك (5 هياكل) — **لم يُلمس** ✓ · لا يمكن تمييز تحميل التصنيفات عن تحميل المنتجات في CSS (نفس markup) ⇒ **بلا تغيير** ✓ (موثّق في §38)

## 23) EMPTY INDEX

**مُختبر بمحاكاة رد فارغ داخل جلسة الفحص** (`categories`/`brands` → `items: []`):
«لا توجد تصنيفات حاليًا» / «لا توجد علامات حاليًا» + «ستظهر هنا بمجرد إضافتها في المتجر.» داخل حالة 3F (560×198) ✓ — بلا JS جديد ✓ وبلا كسر التكوين ✓

## 24) ERROR

محاكاة فشل `categories` ⇒ «تعذّر تحميل البيانات» + «إعادة المحاولة» داخل حالة 3F ✓ · **لم يتغيّر أي سلوك/معالجة** ✓

## 25) FOOTER RELATIONSHIP

- **لا `min-height` ولا حشوات مصطنعة** ✓ · `footerTop` قبل/بعد: **568.97 (390) · 614.05 (1366) — متطابق** ✓ · `documentHeight` **900** في الحالتين ✓
- المحتوى القصير يبقى طبيعيًا، والفراغ أسفل آخر بطاقة هو فراغ الصفحة القصيرة كما كان (لا نُملؤه بمحتوى وهمي) ✓

## 26) RESPONSIVE MATRIX (تصنيفات + علامات × 12 عرضًا)

| العرض | الأعمدة بعد | البطاقة | فائض |
|---|---|---|---|
| 360/390/430/600 | **1** | 336/366/406/576 × 92 | 0 |
| 768 | **2** | 360 × 92 | 0 |
| 769 | 2 | 276 × 96 | 0 |
| 900/1024 | 3 | 276 × 96 | 0 |
| 1280/1366/1440/1920 | 4 | 276 × 96 | 0 |
(المصفوفة الكاملة قبل/بعد لكل عرض ولكل صفحة في §7-9 وفي `probe3e-*.json`)

## 27) OVERFLOW MATRIX

`document.scrollWidth === clientWidth` ⇒ **0 على 12 عرضًا × (تصنيفات/علامات)** ✓ (كان 0 أيضًا قبل — لأن القصّ كان داخل مكوّن `overflow:hidden` لا على مستوى الصفحة؛ الفرق أن البطاقة الثانية **صارت مرئية كاملة** ✓)

## 28) API / REQUEST COUNT (قبل = بعد)

| الصفحة | الطلبات |
|---|---|
| Home | products×2 + categories + brands |
| Categories index | **`categories?limit=48` فقط** |
| Brands index | **`brands?limit=48` فقط** |
⇒ **لا طلب إضافي لكل بطاقة** ✓ والعدّادات من نفس الردّ ✓ (لا caching جديد ✓)

## 29) PRODUCT LISTING REGRESSION

390: بطاقة **177×504** · الشريط **130** · زر الفلاتر موجود · فائض 0 ✓ (مطابق)
1366: الشريط **182 · صفّان** · الأعمدة `411.7+228.75×3` · بطاقة **280×512** · هيدر **74** ✓ (مطابق)
3C: `minPrice=400` ⇒ 1 بطاقة/«1 منتج» ✓ · `offers=1` ⇒ 1 ✓ · `page=2` ⇒ `#/products` + 2 ✓

## 30) PRODUCT CARD GEOMETRY REGRESSION

**لم ألمس أي CSS للبطاقة** ✓ — القياسات: 177×504 (390) · 238×491 (769) · 320×552 (1024) · 280×512 (1366) ✓ (مطابقة تمامًا)

## 31) 3F DARK FILTER REGRESSION

شريحة الفلاتر على الجوال داكنًا: `sheetBg #121b2b` · `checkBg #121b2b` · مفتوحة ✓ (مطابق) ✓

## 32) HOME REGRESSION (390)

`hero ✓` · `homeCards 5` · **`homeTaxoItems 2`** · **`homeTaxo {390×113, flex}`** · **`homeTaxoItem {138×109, x=240}`** · `homeTaxoIcon ✓` · `headerH 65` · `overflow 0` — **مطابق حرفيًا قبل/بعد** ✓✓ (Home لم يتأثر إطلاقًا)

## 33) HEADER / MENU / SEARCH REGRESSION

جوال **65** · ديسكتوب **74** ✓ (بلا لمس header CSS) · القائمة/البحث/الهيدر لم تُختبر وظيفيًا هنا لأن ملفاتها لم تُلمس ✓

## 34) JS FILES MODIFIED = **0** · 35) BACKEND = **0** · 36) NEW `!important` = **0**

## 37) SCREENSHOTS (62 لقطة قبل/بعد)

المصفوفة الكاملة: categories/brands × 390/768/1024/1366/1920 (فاتح) + 390/1366 (داكن) + longname (360/390/1024/1366 للتصنيفات و390/1366 للعلامات) + zerocount (390/1366) + focus (360/390/1024/1366) + empty_categories/empty_brands + error_categories + loading + home-390.

## 38) DEFERRED

1. **تحميل التصنيفات/العلامات** يعرض هياكل بطاقات منتجات (4×320px) لأن `loadingBlock` مشترك وبلا وسم يميّز الصفحة ⇒ إصلاحه يحتاج JS/markup (ممنوع في 3E) ✓
2. **`categories`+`brands` بلا caching** (طلبان لكل تفاعل في صفحات المنتجات) — خارج النطاق ✓
3. **قواعد ≤640 القديمة** (flex + تمرير أفقي) ما زالت موجودة في `shop.css` لكنها **مُبطَلة فعليًا** لتخصيص أقل من قواعد 3E — تركتها (بلا refactor واسع) وهي موثّقة ✓
4. **`.shop-taxonomy__icon`** (Home) وتوكنزه legacy — تُرك كما اتفقنا في 3F ✓
5. **فراغ أسفل بطاقة أخيرة** في الصفحات القصيرة: سلوك الصفحة الطبيعي (بلا حشو مصطنع) ✓
6. **a11y/حالات** اللوحة والتركيز العام ⇒ 3G ✓

**3D and 3G NOT STARTED.** — STOP بانتظار الموافقة.