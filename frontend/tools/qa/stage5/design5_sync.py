"""PHASE 5 · 40 — مزامنة DESIGN.md مع النظام المُنفَّذ فعليًا (يُشغَّل بعد نجاح كل البوابات)."""
from hermes_tools import patch

D = '/root/alwled/frontend/shop/DESIGN.md'
P = []

# 1) قسم الطباعة: الخط + التوفر + إزالة Qomra
P.append(("""**Display/UI Font:** `Tahoma, Cairo, 'Segoe UI', 'Noto Naskh Arabic', 'Helvetica Neue', Arial, sans-serif` (`--font-sans`) — خط نظامي متاح دائمًا، بلا تحميل ويب.

**Menu Font (scoped):** `'Qomra Arabic'` — مستخدم **في القائمة (drawer) فقط** عبر `--shop-menu-font` في `shop.header.css`؛ ليس خطًا عامًا للواجهة ولا إلزامًا لأي سطح جديد.""",
"""**Display/UI Font (المتجر):** `'Cairo', Tahoma, 'Segoe UI', 'Noto Naskh Arabic', 'Helvetica Neue', Arial, sans-serif` (`--st-font`) — **Cairo أولًا** ومُضمَّن محليًا في المشروع عبر `@font-face` في `base.css` (`assets/fonts/Cairo-Regular.ttf`, `font-weight: 400 700`, `font-display: swap`) ⇒ **متاح لكل زائر ومضمون**، بلا CDN وبلا خطوط Google وبلا اعتماد على نظام الزائر. التوفر مُتحقَّق: `document.fonts` = `Cairo/400 700/loaded`. الحقيقة الكاملة: ملف TTF **واحد** يغطّي المدى 400–700، والأوزان الوسيطة (500/600) قد يُصنّعها المتصفح (synthetic) بدل ملف مستقل.

**القائمة/الدُرج:** كانت `'Qomra Arabic'` عبر `--shop-menu-font` — **أُزيلت بالكامل من واجهة المتجر** (7 مراجع → 0، وحُذف التوكن و`@font-face` الخاص بها) ضمن 5B؛ القائمة تستخدم خط المتجر نفسه. ملفات `QomraArabic-*.woff2` تبقى على القرص بلا أي مرجع من الواجهة. الشعار (teal) أصل صورة محفوظ ولا يتأثر بأي تغيير خط."""))

# 2) الهرم الطباعي → الأدوار الدلالية
P.append(("""### Hierarchy
- **Section title** (700, 19px, 1.35): عناوين الصفحات/الأقسام («كل المنتجات»، «التصنيفات»).
- **Price now** (700, 18px ديسكتوب / 17px جوال): أعلى رقم في البطاقة بعد الصورة — السعر هو ثاني أهم معلومة.
- **Product name** (600, 15px ديسكتوب / 14.5px جوال، سطران كحد أقصى): اسم المنتج في القوائم.
- **Field** (400, 15px): الحقول والقوائم المنسدلة.
- **Body** (400, 14px, 1.6): النص العام والتنقّل.
- **Label / meta** (500, 12.5px): تسميات الأدوات · (400, 12px) العلامة التجارية داخل البطاقة · (700, 11.5px) الشارات.
- **Secondary text** (400, 13.5px): العدّاد، المسار، النص الفرعي للسعر القديم.""",
"""### Hierarchy — أدوار دلالية (5B) بـ`rem` حصراً
كل مقاسات النص في واجهة المتجر **rem-based** (0 قيمة px باقية) ⇒ تكبير النص في المتصفح يعمل فعليًا.

| الدور | التوكن | القيمة | الوزن | الاستخدام |
|---|---|---|---|---|
| Display/Hero | `--st-display` | `clamp(1.0625rem, 0.95rem + 0.6vw, 1.5rem)` | 700 | عناوين الهيرو |
| Page title | `--st-title-page` | `1.25rem` (20px) | 700 | عناوين حالات الفراغ/الخطأ |
| Section title | `--st-title-section` | `1.25rem` (20px) | 700 | «كل المنتجات»، «التصنيفات»، لوحة PDP |
| Card title | `--st-title-card` | `0.9375rem` (15px) — جوال 14.5px | 600 | اسم المنتج في القوائم/التصنيفات |
| Body | `--st-body` | `0.875rem` (14px) | 400 | النص العام، الوصف، تنقّل |
| Body small | `--st-body-sm` | `0.8125rem` (13px) | 400/500 | المسار، العدّاد، روابط التذييل، الشارات النصية |
| Meta/Label | `--st-meta` | `0.75rem` (12px) | 400/500 | العلامة التجارية، تسميات الأدوات، حقائق PDP |
| Caption | `--st-caption` | `0.6875rem` (11px) | 700 | الشارات |
| Price XL | `--st-price-xl` | `clamp(1.5rem, 1.2rem + 1vw, 2rem)` | 700 | سعر PDP (24→32px) |
| Price L | `--st-price-l` | `1.125rem` (18px) | 700 | سعر البطاقة (جوال 17px) |
| Price M | `--st-price-m` | `1rem` (16px) | 700 | سعر صف البحث/الملخّص |
| Old price | `--st-old-price` | `0.8125rem` (13px) | 400 | السعر المشطوب |
| Button | `--st-btn` | `0.875rem` (14px) | 600 | كل أزرار المتجر |
| Input | `--st-input` | `1rem` (16px) | 400 | حقول البحث/الفلاتر/الكمية (≥16px يمنع تكبير iOS) |

**ارتفاعات الأسطر (3 عائلات):** `--st-lh-tight: 1.3` · `--st-lh-normal: 1.55` · `--st-lh-loose: 1.75` — وكل `line-height` بالـpx حُوّل إلى `rem` (0 px باقية) ليتناسب مع تكبير النص."""))

# 3) القاعدة: السعر
P.append(("""**The Price-First Rule.** بعد الصورة، السعر الحالي هو أكبر نص في البطاقة (18px/700)؛ السعر القديم أصغر (13.5px) وبمحاذاة خافتة ومشطوب.""",
"""**The Price-First Rule.** بعد الصورة، السعر الحالي هو أكبر نص في البطاقة (`--st-price-l` 18px/700 · جوال 17px)؛ السعر القديم `--st-old-price` (13px) خافت ومشطوب. سلّم السعر موحّد بثلاثة أدوار فقط: L (بطاقة) · M (صف البحث) · XL (PDP)."""))

# 4) الأزرار
P.append(("""### Buttons
- **Shape:** نصف قطر 8–10px، ارتفاع **44px** (والشبح 44px)، حشوة أفقية 12px (الشبح 10px).
- **Primary:** أزرق المتجر ممتلئ + نص أبيض (`#FFFFFF` على `#1F5FBF` = 6.09:1 في الثيمين)؛ hover `#1A54A8` · active `#164A93`.""",
"""### Buttons (5C)
- **الشكل:** نصف قطر 8–10px، حشوة أفقية 12px (الشبح 10px).
- **الارتفاعات (دلالية):** `2.5rem` (40px) للأزرار المضغوطة (`btn--sm`) · **`2.75rem` (44px)** للأزرار العادية (لمس) · **`3rem` (48px)** لـCTA الشراء (`btn--lg`) — بارتفاع `min-height` لا `height` للصفوف العادية ⇒ النص المتكبّر يوسّع الزر بدل أن يُقصّ.
- **الطباعة:** `--st-btn` (14px) بوزن 600 لكل أزرار المتجر، مع `white-space: normal` + توسيط ⇒ لا قصّ عند 200%.
- **Primary:** أزرق المتجر ممتلئ + نص أبيض (`#FFFFFF` على `#1F5FBF` = 6.09:1 في الثيمين)؛ hover `#1A54A8` · active `#164A93` — **على كل أسطح الزبون** (الكتالوج · PDP · السلة · الدفع · الطلبات · الإشعارات · الحساب · نافذة المصادقة) بعد إزالة teal في 5C/5D."""))

# 5) الحقول
P.append(("""- **Style:** سطح `--store-surface` + حدّ 1px `--store-border` + نصف قطر 8px + ارتفاع 44px + حشوة `8px 12px`؛ نص 15px.""",
"""- **Style:** سطح `--store-surface` + حدّ 1px `--store-border` + نصف قطر 8px + ارتفاع 44px + حشوة `8px 12px`؛ نص **`--st-input` = 16px** (كل حقول المتجر، يمنع تكبير iOS التلقائي عند 15px)."""))

# 6) القاعدة الجديدة: أزرق المتجر لكل CTA
P.append(("""**The Locked Teal Rule.** الـteal (`#14B8A6`) أصل شعار محفوظ — لا يُستعمل كلون واجهة ولا يُعاد تلوينه. أي teal متبقٍ في CSS القديم هو **بقايا legacy** لا قاعدة تصميم (انظر Do's and Don'ts).""",
"""**The Locked Teal Rule.** الـteal (`#14B8A6` وأسرته) أصل شعار محفوظ — لا يُستعمل كلون واجهة ولا يُعاد تلوينه. **اعتبارًا من 5C/5D: 0 مرجع `--brand-*` في CSS واجهة المتجر** (56 مرجعًا حُوّل إلى أزرق المتجر)، و0 عنصر teal في فحص أسطح الزبائن (فاتح وداكن). أي teal متبقٍ في المشروع يخصّ **لوحة الإدارة** (ملفات مشتركة: `layout.css`/`components.css`/`tokens.css`) وهو خارج نطاق المتجر.

**The Store-Blue CTA Rule.** كل فعل أساسي في المتجر يخرج من `--store-primary` عبر إعادة تعيين واحدة في جذر المتجر: `html .shop-body { --primary/-hover/-active/-soft = --store-* · --ring = rgba(31,95,191,.35) · --border-brand = --store-primary }`. الألوان الدلالية (`--store-danger/success/warning/info`) **لا** تُحوَّل إلى أزرق."""))
