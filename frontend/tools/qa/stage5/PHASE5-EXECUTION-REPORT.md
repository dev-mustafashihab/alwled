# PHASE 5 — IMPECCABLE · TYPOGRAPHY + UI SYSTEM · EXECUTION REPORT

**Backend/API/DB/routes = 0 · Dependencies installed = 0 · JS modified = 0 · New !important = 0 · HTML = 0 · PRODUCT.md = 0**

---

## 1. 5B — TYPOGRAPHY FOUNDATION

**الملفات:** `assets/css/tokens.css` (إضافة طبقة `--st-*` فقط، بلا مساس بـ`--store-*` أو `--fs-*`) · `shop.css` · `shop.header.css` · `shop.home.css` · `shop.search.css` · `shop.filters.css`.

- `--st-font: 'Cairo', Tahoma, 'Segoe UI', 'Noto Naskh Arabic', 'Helvetica Neue', Arial, sans-serif` وتُطبَّق على جذر المتجر `.shop-body { font-family: var(--st-font) }`.
- **Qomra أُزيل بالكامل من الواجهة:** 7 مراجع `--shop-menu-font` → 0 · حذف تعريف التوكن · حذف 3 كتل `@font-face`. ملفات `QomraArabic-*.woff2` تبقى على القرص بلا مرجع.
- طبقة الأدوار: 14 دور نص + 3 عائلات `line-height` + 5 توكنز مسافات صغيرة — كلها `rem`.
- **كل `font-size` في ملفات المتجر → rem** (0 قيمة px باقية) · **كل `line-height: Npx` → rem** (0 px باقية).
- **`letter-spacing` غير الصفري → `normal`** (4 مواضع: `.shop-brand__text strong` ×2 · `.shop-drawer__group-title` · `.shop-search-sheet__label`).
- **`text-transform: uppercase` → `none`** (2 مواضع: الهيدر + البحث).
- الحقول → `--st-input` (16px) · أزرار → `--st-btn` (14/600).
- اسم البطاقة على الجوال أُبقي **14.5px** (0.90625rem) عن قصد لحفظ هندسة 3D المقفلة (177×327).

## 2. TYPOGRAPHY BEFORE → AFTER (computed)

| الدور | قبل | بعد |
|---|---|---|
| Section title | 19px (كتالوج) / 20px (رئيسية) | **20px** (دور واحد) |
| Card title | 14.5 (جوال) / 15 / 16 (بحث) | **15px** (جوال 14.5 محفوظ) |
| Card meta | 12 / 12.5 (PDP) | **12px** |
| Toolbar label / count | 12.5 / 13.5 | **12px** |
| Old price | 13.5px | **13px** |
| Body/Prose | 15px | **14px** |
| Badge | 11.5px | 11px (`--st-caption`) |
| Button (PDP/CTA) | 15px | **14px / 600** |
| Inputs | 13 / 15 / 16px | **16px** |
| Prices | 16 / 17 / 18 / 24 / 32 | **L 18 (جوال 17) · M 16 · XL clamp 24→32** |
| letter-spacing | 4 قيم | **normal** |
| uppercase | 2 | **0** |
| font-size px | ~120 تعريف | **0** |
| line-height px | ~40 تعريف | **0** |

## 3. CAIRO — التنفيذ والتوفر (حقيقة كاملة)

- **مُضمَّن محليًا في المشروع**: `@font-face` في `base.css` → `assets/fonts/Cairo-Regular.ttf` بـ`font-weight: 400 700` و`font-display: swap` ⇒ **مضمون لكل زائر**، لا يعتمد على نظام الزائر.
- **مُتحقَّق وقت التشغيل:** `document.fonts` = `Cairo/400 700/loaded` · `document.fonts.check('16px "Cairo"')` = true.
- **الحقيقة:** ملف TTF **واحد** يغطّي المدى 400–700 ⇒ الأوزان الوسيطة (500/600) قد **يُصنّعها المتصفح** (synthetic bold) بدل ملف مستقل.
- **لا** تنزيل خطوط · **لا** CDN · **لا** Google Fonts · **لا** تبعيات.

## 4. QOMRA BEFORE → AFTER

| | قبل | بعد |
|---|---|---|
| مراجع `var(--shop-menu-font)` | 7 | **0** |
| تعريف التوكن | موجود | محذوف |
| كتل `@font-face` | 3 | **0** |
| الخط المحسوب في القائمة/الدُرج | `Qomra Arabic` | **Cairo** |

## 5. 5B QA

الهندسة المقفلة سليمة: هيدر 64/73 (65/74 مع الحدّ) · بطاقة 177×327 و280×357 · معرض PDP 586/366 · CTA 48 · فائض أفقي 0 · console 0 · failed 0. **انحراف واحد اكتُشف وأُصلح:** بطاقة 390 صارت 329 (+2px) بسبب توحيد اسم/سعر البطاقة ⇒ أُعيد اسم الجوال إلى 14.5px ⇒ **327 مطابق**.

## 6. 5C — CONTROLS / BUTTONS / FORMS

- **إعادة تعيين واحدة على جذر المتجر** (`html .shop-body`) ⇒ كل مكوّنات المتجر تخرج من أزرق المتجر:
  `--primary: var(--store-primary)` · `--primary-hover: #1A54A8` · `--primary-active: #164A93` · `--primary-soft` · `--ring: rgba(31,95,191,.35)` · `--border-brand: var(--store-primary)`.
- **الأزرار:** `min-height` 44px (عادي) · 48px (CTA شراء) · 40px (مضغوط) + `font-size: --st-btn` (14) + وزن 600 + `white-space: normal` + توسيط ⇒ لا قصّ عند 200%. الصفوف العادية `height: auto` (لا ارتفاع ثابت يُقصّ النص).
- **الحقول:** 16px في كل حقول المتجر (بحث/فلاتر/كمية).
- **المسار:** منطقة نقر موسّعة (`::after inset: -12px -4px`) ⇒ الهدف العملي ~43px بدل ~21px.
- **الهيدر:** `min-height` + `flex-wrap` (64/72px عند 100% كما هي، وينمو بدل أن يفيض عند 200%).
- الألوان الدلالية (`danger/success/warning/info`) **لم تُلمس**.

## 7. CTA TEAL → BLUE

| | قبل | بعد |
|---|---|---|
| مراجع teal في CSS المتجر | 56 | **0** |
| عناصر teal على أسطح الزبائن (فاتح) | 7 أسطح + نافذة المصادقة | **0** |
| عناصر teal في الوضع الداكن | — | **0** |
| تباين نص CTA | 3.7:1 | **6.09:1** |

## 8. CONTROLS / FORMS AFTER

- ارتفاعات: **40 / 44 / 48** (دلالية: مضغوط / لمس / CTA شراء) — لا توحيد أعمى على 48.
- زر نافذة المصادقة: **112×40 → 101×47** (لمس ≥44 ✓).
- حقول الجوال ≥16px ⇒ لا تكبير iOS تلقائي.
- زر إغلاق النافذة (`modal__close`) = 34×34 ⇒ **DEFER** (تجنّب تغيير تخطيط النوافذ بعد البوابة).

## 9. 5C QA

هندسة مطابقة (177×327 / 280×357 / 586×366 / 48) · فائض 0 · console 0 · failed 0 · **فائض 200% عند 1366: 48px → 0** (إصلاح الهيدر).

## 10. 5D — CARDS / SURFACES / RHYTHM

- هجرة teal الكاملة في CSS المتجر (56 مرجع `--brand-*` → أزرق المتجر) — تشمل حدود hover للبطاقة، تنقّل نشط، chips البحث، تدرّجات الهيرو.
- **توكنزة المسافات الدخيلة** (43 موضعًا: 2/6/10/14/18px → `--st-space-*`) بلا أي تغيير بصري.
- سعر PDP → `var(--st-price-xl)` (توكن دلالي بدل clamp مكرّر).

## 11. CARDS / SURFACES / RHYTHM AFTER

- سلّم السعر موحّد (L/M/XL) · DNA البطاقة كما هو (3D مقفل) · كثافة Home/Listing/Related/Search محفوظة كما هي.
- لا ظلال زخرفية جديدة · لا استدارة إضافية · الحدّ 1px يبقى أداة الفصل الأولى.
- **لا ظل/inset teal في أي قاعدة متجر** — المتبقيان في المشروع داخل ملفات مشتركة (لوحة الإدارة) = خارج نطاق المتجر.

## 12. 5D QA

هندسة مطابقة تمامًا لـ5C · فائض 0 في 13 عرضًا · console 0 · failed 0.

## 13. 5E — PAGE REFINEMENT

- `min-width: 0` على حاويات النص (sections/product/buy/card body/taxonomy) ⇒ إلغاء قصّ 200% في أقسام الرئيسية.
- `overflow-wrap: break-word/anywhere` على العناوين وقيم الحقائق ⇒ لا نص يفيض.
- روابط التذييل: `min-height: 44px`.
- توحيد دور عنوان القسم (تم في 5B) · حالات loading/empty/error بقيت على أدوارها ودلالاتها (`aria-busy`/`status`/`alert`).

## 14. FOOTER / BREADCRUMB / STATES

- **التذييل:** القصّ المُبلَّغ عند ≤600 **تحقّق أنه إيجابية كاذبة للكاشف** (لقطة + تشخيص: الطفل الوحيد هو التسمية القصيرة، ولا عنصر داخلي يفيض؛ `overflow-x: hidden` على `summary` لأجل 1px قديم) ⇒ **لا نص مقصوص فعليًا**.
- **المسار:** اختبار النقر 3/3 على ±11px ⇒ الهدف العملي ~43px.
- **الحالات:** loading/empty/error بدون تغيير وظيفي؛ الطباعة موحّدة عبر الأدوار.

## 15. 5E QA

هندسة مطابقة (177×327 / 280×357 / 586×366 / 48) · فائض 0 · console 0 · failed 0.

## 16. 5F — RESPONSIVE SUMMARY

**13 عرضًا (360 · 390 · 430 · 600 · 768 · 769 · 900 · 1023 · 1024 · 1280 · 1366 · 1440 · 1920) × 6 صفحات (Home · Products · Categories · Brands · PDP متاح · PDP غير متاح) = 78 قياسًا · فائض أفقي = 0 في كلها.**
عروض الانتقال 600/768/769/1023/1024 = 0 ✓ · docH: Home 2729–3379 · PDP 1654–2010.

## 17. LIGHT / DARK

الداكن عند 390/768/1024/1366 × 3 صفحات: **فائض 0 · teal 0 · فائض 200% = 0** — لا جزر بيضاء (الأسطح كلها توكنز `--store-*`).

## 18. 200% TEXT-SIZE

- **قبل:** بلا أثر إطلاقًا (typography px). **بعد:** تكبير فعلي مقيس — بطاقة القوائم **327→464px** (390) و**357→469px** (1366) · المسار 42→82×42 · شريط الأدوات 168px · docH ينمو.
- **فائض أفقي = 0** عند 390/768/1024/1366 (فاتح + داكن) · لا قصّ نص · لا تداخل تحكمات · لا CTA غير قابل للوصول.
- لم يُثبَّت أي ارتفاع ثابت لإجبار النجاح: الأزرار `min-height` + التفاف، الهيدر `min-height` + `flex-wrap`.

## 19. ACCESSIBILITY

- أهداف اللمس: أزرار المتجر ≥44 (CTA 48 · المصادقة 47) · حقول 44 · صفوف اختيار 44 · المسار ~43 (بدل ~21).
- تباين CTA: **6.09:1** (بدل 3.7:1) · الألوان الدلالية محفوظة.
- لا قصّ نص · لا تداخل · 0 فائض · console 0.
- **لا ندّعي أي شهادة WCAG.**

## 20. REMAINING TEAL — CLASSIFICATION

| الموضع | التصنيف |
|---|---|
| أي قاعدة CSS في `shop/assets/css/*` | **0** ✓ |
| أسطح الزبائن (7 أسطح + نافذة المصادقة) فاتح/داكن | **0** ✓ |
| شعار المتجر (أصل صورة) | **مقصود ومحفوظ** ✓ |
| `layout.css:45` (ظل) · `components.css:1607` · `tokens.css:70/246` | **نطاق لوحة الإدارة** (ملفات مشتركة) — خارج نطاق المتجر |
| سلّم `--brand-*` في tokens.css | تعريف فقط (يستخدمه الأدمن) — لا استخدام في المتجر |

## 21. LOCKED PHASE 1–4 MEASUREMENTS (بعد Phase 5)

| العنصر | المقفل | الحالي |
|---|---|---|
| هيدر جوال / ديسكتوب | 65 / 74 | **65 / 74** ✓ (inner 64/73) |
| بطاقة قوائم 390 / 1366 | 177×327 / 280×357 | **مطابق** ✓ |
| تصنيفات 390 / 1366 | 366×92 / 276×96 | **مطابق** ✓ |
| بطاقة الرئيسية 390 / 1366 | 177×321 / 280×512 | **177×321 / 280×510** (−2px تقريب rem) |
| صف نتيجة البحث / الحقل | 90 / 48 | **90 / 48** ✓ |
| معرض PDP 390 / 1366 | 366 / 586 | **مطابق** ✓ |
| زر الشراء PDP | 48px | **48px** ✓ |
| فائض أفقي | 0 | **0** ✓ |

## 22. EXACT SOURCE FILES MODIFIED (6)

`assets/css/tokens.css` → `aa4fd2ad5640df07b2f3869278c3485d`
`shop/assets/css/shop.css` → `f6a11b40e17d75f7acce75cda143951a` (بعد إصلاح حقول 5F)
`shop/assets/css/shop.header.css` → `e2927db08da5427d7cd326e71f54bf3e`
`shop/assets/css/shop.home.css` → `d6707c81a4b2156d81940787a0149abd`
`shop/assets/css/shop.search.css` → `29ea29efa01ec55ad51799d82c8f5c05`
`shop/assets/css/shop.filters.css` → `133e82b236a26d2f18c588284ea92177`
**+ توثيق:** `shop/DESIGN.md` → `9b25a34de4783ed247d1a112be58d6d8`

**غير معدّل (مُتحقَّق بالهاش):** `components.css` (مطابق للنسخة الاحتياطية) · `shop/index.html` · كل ملفات `shop/assets/js/*` · `PRODUCT.md` · backend/API/DB/routes.

## 23. DESIGN.md CHANGES

372 → **414 سطرًا**: قسم Typography أُعيد كتابته (جدول الأدوار الدلالية + حقيقة Cairo والتوفر + إزالة Qomra) · The Price-First Rule محدّثة · قسم Buttons (5C: الارتفاعات الدلالية + 14/600 + min-height) · Inputs 16px · The Locked Teal Rule محدّثة + **The Store-Blue CTA Rule** جديدة · Layout: الطباعة المتجاوبة (clamp في 3 مواضع) + سلوك 200% + المسافات + الأنصاف/الظلال + سطر خط القائمة · Don't الخاص بـQomra محدّث.

## 24. CONSOLE / NETWORK / OVERFLOW

console errors = **0** · failed requests = **0** · فائض أفقي = **0** (78 قياسًا فاتح + 12 قياسًا داكن + 4 قياسات 200% + مصفوفات المراحل).

## 25. DEPENDENCIES / !IMPORTANT / JS

| البند | النتيجة |
|---|---|
| تبعيات مثبّتة | **0** |
| `!important` (shop.css/header/home/search/filters) | 5 / 14 / 1 / 0 / 0 → **بدون تغيير** ⇒ جديد = **0** |
| JS معدّل | **0** (md5 مطابق) |
| HTML معدّل | **0** |
| backend/API/DB/routes | **0** |
| PRODUCT.md | **0** |

## 26. DEFERRED ITEMS + REASONS

1. **`modal__close` = 34×34** — تحت 44px؛ أُجّل لتجنّب تغيير تخطيط النوافذ بعد نجاح البوابة النهائية (≥24px مقبول معيارياً).
2. **قيم مقاسات فردية متبقية** (11.5 / 12.5 / 13.5 / 19px داخل أسطح مقفلة) — أُبقيت لحماية الهندسة المقفلة؛ تُطوى في الأدوار عند إعادة اعتماد الهندسة.
3. **مُنسّق العملة** — يقع في `shop.app.js` (خارج الملفات المسموحة) ⇒ مؤجّل كما في 4B.
4. **`.btn--busy` المرئي** — التغذية الراجعة قائمة فعلًا (تعطيل + توست) ⇒ مؤجّل.
5. **ظل/inset teal + سلّم `--brand-*`** — نطاق لوحة الإدارة في ملفات مشتركة ⇒ خارج نطاق المتجر.
6. **بطاقة الرئيسية 512→510px** (−2px) — تقريب تحويل rem؛ غير مؤثّر بصريًا.
7. **فراغ الشبكة مع منتجين فقط** — بيانات اختبارية (لا صور/وصف) ⇒ ليس عيب تصميم.
8. **وضوح سهم القوائم المنسدلة** — ملاحظة غير مُتحقَّقة (السهم SVG محلي موجود في CSS) ⇒ لم تُرفع كعيب.


---

## 27. تدقيق مستقل بعد التسليم (adversarial) — نتيجة + تصحيح

**أداة:** `independent_audit.py` (سجل شبكة كامل · قياس خطوط على canvas · قواعد CSS · ألوان دلالية · أسطح إضافية · 200% للأسطح الناقصة).

| الفحص | النتيجة |
|---|---|
| طلبات الشبكة | 45 طلبًا · **نطاق واحد فقط** `panel.fahd-car.cloud` · **0 طلب خارجي** (لا CDN ولا Google Fonts) |
| طلبات الخطوط | **1 فقط**: `.../alwled/assets/fonts/Cairo-Regular.ttf` · **0 طلب لأي ملف Qomra** |
| هل Cairo هو المُصيَّر فعلًا؟ | **نعم** — عرض النص بخط Cairo = **513px** = عرض السلّم المُعلن (513) بينما Tahoma = 493 وArial = 508 ⇒ الخط المُستخدم فعلًا هو Cairo لا مجرد اسم في السلّم |
| الألوان الدلالية | شارة الخصم `badge--danger` = خلفية `rgb(254,242,242)` ونص `rgb(153,27,27)` = **أحمر محفوظ** · التوكنز `#dc2626/#16a34a/#d97706/#2563eb` سليمة |
| متغيّرات CSS | **110 قاعدة** تستخدم `var(--st-*` ⇒ **0 قاعدة خارج `.shop-body`** (لا متغيّر غير محلول) |
| 200% للأسطح الناقصة | الفلاتر (sheet 390×738، بلا فائض داخلي) · القائمة (صف 50→64) · البحث (صف 90→178) · نافذة المصادقة — **فائض أفقي 0 في كلها** |
| مسارات إضافية | `#/verification` = حالة «تسجيل الدخول مطلوب» · `#/payment` و`#/order` = صفحة 404 مقصودة · كلها فائض 0 · console 0 |
| نافذة المصادقة | بلا حقول إدخال (3 خيارات) · أزرار 101×47 (390) و118×44 (1366) ≥44 ✓ · `modal__close` = 34×34 (DEFER) |

### ⚠️ عيب حقيقي اكتشفه التدقيق + تصحيح تقريري

- **التقرير السابق ادّعى أن كل حقول المتجر 16px — وهذا كان غير صحيح لحقول الفلاتر:** القياس أظهر `#shop-filter-search` والحقول/القوائم = **15px** لأن قاعدة `.shop-toolbar select.select` (والقواعد الأقدم) تغلبت على قاعدتي بسبب الأولوية/الترتيب.
- **الإصلاح (جراحي):** قاعدة حقول متأخرة في نهاية الكتلة (`.shop-body input:not([type=checkbox]):not([type=radio]), select, textarea, .input, .select, .textarea, .shop-toolbar input/select, .shop-filters__field input/select { font-size: var(--st-input) }`) + تحويل `.shop-toolbar select.select` إلى `var(--st-input)`.
- **بعد الإصلاح (مُتحقَّق):** `#shop-filter-search` 358×44 / 412×44 = **16px** · `category/brand/sort` = **16px** · `minprice/maxprice` = **16px** · حقول البحث/الكمية = 16px — على 390 و1366 · **console 0 · فائض 0**.
- **الهندسة بعد الإصلاح:** هيدر 390×64 و1200×73 · بطاقة 177×327 و280×357 · شريط الأدوات 390×128 و1168×180 · زر الفلاتر 44 ✓ — مطابقة للمقفلات.

---

PHASE 5 COMPLETE.
PHASE 5 LOCKED.
READY FOR NEXT PHASE.
