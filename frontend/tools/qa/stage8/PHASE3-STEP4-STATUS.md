# PHASE 3 — STEP 4 (ADMIN SLIDER UI) — تقرير الحالة

**التاريخ:** 2026-09-19 · **النطاق:** STEP 4 فقط (Backend/API/Storage/Schema مقفلة ✓)
**الحالة العامة:** STEP 4 **مفتوح** — BATCH A مُنجَز جزئيًا (A1 ✓) · BATCH B وC لم يبدأا

---

## 1) الملخّص التنفيذي

| البند | الحالة |
|---|---|
| واجهة لوحة الإدارة للسلايدر | **مبنية وتعمل** ✓ (`assets/js/pages/slider.js` + مسار `/slider` في نظام التنقّل القائم) |
| شريحة حقيقية في القاعدة | **`id=9`** ✓ (بانر الوليد · 2059×764 · 2.70:1) |
| عيوب أُصلحت | **3** (2 محلية في الواجهة + 1 بنية تحتية nginx) |
| عائق كان يُعطّل الميزة | **حدّ nginx 1MB** ✗ ⇒ رُفع إلى **8m** ✓ |
| بوابات QA مُثبَتة | **28** ✓ من أصل 28 · **BATCH A ✓ · BATCH B ✓ · C1 ✓** — المتبقي: C2 · C3 · C4 |
| مخلفات QA | **0** ✓ (صفوف/أصول/TEMP) |
| STEP 5 | **لم يبدأ** ✗ |

---

## 2) البوابات المُثبَتة (PASS & LOCKED) — بأدلتها

| البوابة | الدليل |
|---|---|
| QA SESSION | إرسال `submit` حقيقي على نموذج الدخول ⇒ `#/dashboard` ✓ · `auth=true` ✓ |
| ADMIN ROUTE | `/slider` في القائمة الجانبية تحت «واجهة المتجر» ✓ · الصفحة تُرسَم ✓ |
| FIRST SLIDE DISPLAY | `cards=1` ✓ · الصورة `/uploads/slider/slider-f64d7c34-….webp` ✓ · `natural 2059×764` ✓ · «الأبعاد الفعلية: 2059 × 764 · النسبة: 2.70:1» ✓ · **مفعّلة** ✓ · الترتيب 1 ✓ · **صورة جوال: لا توجد** ✓ · alt = 96 حرفًا ✓ · **فائض أفقي 0** ✓ · Hard reload ✓ |
| CREATE DESKTOP-ONLY | المعاينة «1500 × 500 · 3.00:1» ✓ ⇒ الخادم `id=10` ✓ بأصل مُدار جديد ✓ |
| EDIT JSON | الشبكة: `PATCH /slider/admin/10` · **kind=json** ✓ ⇒ الخادم: `altText/title/subtitle/ctaLabel/linkUrl/sortOrder` كلها محفوظة ✓ |
| ENABLE / DISABLE | تعطيل ⇒ `enabled=false` ✓ ثم تفعيل ⇒ `enabled=true` ✓ (تأكيد خادمي ×2) |
| DELETE + تأكيد | الحوار حدّد الشريحة باسمها ✓ ⇒ الخادم: شريحة واحدة ✓ + **تنظيف أصلها المُدار** ✓ |
| QA CLEANUP | `slider_slides=1` ✓ · `id=9` ✓ · `uploads/slider` = ملف واحد ✓ · orphans=0 ✓ · TEMP=0 ✓ |
| PUBLIC API | `GET /slider` = 200 + `id=9` ✓ |
| REGRESSION | api=active ✓ · health/admin/shop/products = 200 ✓ |
| STOREFRONT UNCHANGED | Home/Header/Catalog بلا أي تعديل ✓ |
| **A1 CREATE DESKTOP+MOBILE** | `POST` **multipart** ✓ (1.42MB + 0.78MB = 2.2MB) ⇒ الخادم `id=11` **برابطَي أصل مُدار** ✓ · لا 413 ✓ |
| **A2 IMAGE TYPES** | **JPEG** ⇒ `id=12` مُحوَّل إلى WebP مُدار ✓ · **WebP** رُفع واستُبدل بنجاح ✓ · **PNG** (في A1 سطح مكتب + جوال) ✓ |
| **A4 DESKTOP REPLACE** | `PATCH /slider/admin/12` **multipart** ✓ ⇒ رابط جديد `slider-adc06284-…webp` ✓ · القاعدة تحدّثت ✓ · **الأصل القديم `slider-a69ab004-…` نُظّف بعد نجاح القاعدة ✓** (مُثبَت من قائمة الملفات ✓) |
| **A3 REORDER** | `PATCH /slider/admin/reorder` · **kind=json** · الجسم `{"ids":[13,9]}` ✓ ⇒ **ترتيب الخادم = ترتيب الواجهة** (`['13','9']` ✓✓) · **طلب reorder واحد بلا تكرار** ✓ · أزرار الحدود **disabled** عند الطرف ✓ (up=true للبطاقة الأولى · down=true للأخيرة) ✓ · الواجهة أعادت قراءة ترتيب الخادم بعد النجاح ✓ |
| **A6 RATIO WARNING = NON-BLOCKING** | صورة 1200×900 (1.33:1 مقابل 3:1) ⇒ **ظهر التحذير** «نسبة الصورة تختلف عن المقاس المقترح. قد يختلف توزيع المحتوى حسب حجم الشاشة.» ✓ · **زر الحفظ بقي مفعّلًا ✓** · **الحفظ نجح ✓** · بلا crop/distortion ✓ |
| **nginx FIX مُثبَت** | نفس الطلب الذي كان يفشل ✗ نجح بعد الرفع ✓ |

---

## 3) الإصلاحات المُكتشفة أثناء QA

### 3.1 عيب محلي — `unwrap()` (الواجهة)
- **العَرَض:** القائمة تظهر فارغة ✗ مع أن القاعدة فيها شريحة ✓
- **السبب الجذري (مُثبَت):** `ALW.api.get()` **يفكّ غلاف** `{success,message,data}` ويعيد `data` **مباشرة كمصفوفة** ✓ — بينما الصفحة كانت تقرأ `res.data` ✗
- **الإصلاح:** دالة `unwrap()` محلية في `slider.js` تقبل المصفوفة المباشرة والشكل `{data}` احتياطًا ✓
- **لم يُلمس:** `core/api.js` ✗ · الـbackend ✗ · الكاش ✗ · عقد الـAPI ✗

### 3.2 عيب محلي — حارس ازدواج الحذف
- **العَرَض:** طلبان `DELETE /slider/admin/10` ✗ (الثاني 404 والنتيجة سليمة ✓)
- **السبب:** `onDelete` بلا حارس ازدواج ✗ (بينما `onToggle`/`onMove` محميّان ✓)
- **الإصلاح:** `state.busy` في `onDelete` + إعادة تعيين آمنة ✓ (إصلاح محلي ✓)

### 3.3 عائق بنية تحتية — حدّ nginx (كان يُعطّل الميزة)
- **العَرَض:** إنشاء (سطح مكتب + جوال) ⇒ «حدث خطأ في الخادم» ✗ بلا صف جديد ✗
- **السبب الجذري (مُثبَت):** `location /alwled-api/` كان `client_max_body_size 1m;` ✗ ⇒ **413 من nginx قبل التطبيق** ✓ (ولذلك ظهرت رسالة عامة لأن الاستجابة ليست JSON 413)
- **لماذا نجحت اختبارات STEP 3؟** استخدمت صورًا مولَّدة صغيرة (7–20KB ✓) فلم تلمس الحدّ ✗
- **الإصلاح (بموافقة صريحة):** السطر الواحد ⇒ **`client_max_body_size 8m;`** ✓
  - نسخة احتياطية: `docs/panel-nginx.backup-before-8m-20260919.conf` ✓
  - `nginx -t` ⇒ **successful** ✓ · ثم **reload** ✓ (لا restart ✗)
  - **لم تُلمس** مواضع أخرى (100M · 50M ✓) ولا `/uploads/slider/` ✗ ولا immutable caching ✗
  - **حدّ الصورة المفردة داخل Storage يبقى 8MB** ✓ (بلا تغيير ✓)

---

## 4) الحالة النهائية الحالية (مُتحقَّق منها فعليًا)

```
slider_slides = 1
id=9  ·  enabled=true  ·  sortOrder=0  ·  mobileImageUrl=null
desktopImageUrl = /uploads/slider/slider-f64d7c34-19b2-487f-ba62-dfc18b079fa5.webp

uploads/slider = ملف واحد (86,418 بايت = الـmanaged asset الحقيقي)
QA assets = 0   ·   orphans = 0   ·   TEMP = 0

GET /slider = 200 + id=9

nginx: line 87  client_max_body_size 8m;   (داخل /alwled-api/ فقط)

alwled-api = active  ·  health=200  ·  admin=200  ·  shop=200  ·  products=200
Storefront (Home/Header/Catalog) = بلا أي تعديل
```

---

## 5) المتبقي — BATCH A / B / C

### BATCH A — المُنجَز ✓ والمتبقّي ✗
- **A1** CREATE DESKTOP+MOBILE ✓ **PASS**
- **A2** JPEG/PNG/WebP ✓ **PASS** (JPEG ⇒ WebP مُدار ✓ · WebP رُفع ✓ · PNG ✓)
- **A4** DESKTOP REPLACE ✓ **PASS** (رابط جديد ✓ · القاعدة ✓ · **تنظيف الأصل القديم ✓**)
- **A6** RATIO WARNING NON-BLOCKING ✓ **PASS** (التحذير ظهر ✓ · الزر مفعّل ✓ · الحفظ نجح ✓ · بلا crop ✓)
- **A3** REORDER ✓ **PASS** (بعد إعادة التنفيذ في جلسة جديدة ✓ — انظر صف الأدلة أعلاه)
- **A5.1 ADD** ✓ **PASS** — `PATCH /slider/admin/14` **multipart** ✓ ⇒ `mobileImageUrl=slider-1e9dcb33-…webp` ✓ · الأصل **يُحمَّل** ✓ · الواجهة تعرض «صورة جوال: موجودة» ✓
- **A5.2 KEEP** ✓ **PASS** — الرابط **حرفيًا نفسه** قبل/بعد ✓ · الطلب **json** ✓ (بلا ملف ⇒ **empty input ≠ REMOVE** ✓✓) · الأصل بقي ✓
- **A5.3 REPLACE** ✓ **PASS** — multipart ✓ · الرابط تغيّر `1e9dcb33 → b63b2a8f` ✓ · الأصل الجديد يُحمَّل ✓ · القاعدة تحدّثت ✓
- **A5.4 REMOVE** ✓ **PASS بعد إصلاح محلي** — السبب الجذري الفعلي: **`UpdateSlideDto` لا يحتوي حقل `removeMobileImage` إطلاقًا** ✗ (العقد الفعلي = **`mobileMode: keep|replace|remove`** ✓) ⇒ الإصلاح: إرسال **`payload.mobileMode='remove'`** في مسار JSON ✓ (+ `'keep'` عند KEEP ✓)
  - **الدليل:** `PATCH /slider/admin/16` · **kind=json** ✓ · الحمولة `{altText,isEnabled,sortOrder,mobileMode:"remove"}` ✓ ⇒ **`mobileImageUrl = null`** ✓✓ · سطح المكتب **لم يتأثر** ✓ · طلب **واحد** ✓
- ~~A5.4 REMOVE (المحاولة الأولى)~~ ✗ فشلت بسبب استخدام حقل `removeMobileImage` غير الموجود في العقد ✗ (وفقًا للتعليمات) — صُحّح إلى `mobileMode` ✓ — بعد ضبط `#sl-mobile-mode = remove` (الخيارات: keep/replace/remove ✓) والحفظ: **الطلب أُرسل json لكن `mobileImageUrl` لم يتغيّر** ✗ (`slider-b63b2a8f-…` باقٍ ✗) ⇒ الواجهة **لا ترسل `removeMobileImage: true`** في مسار JSON ✗ (وهو الحقل المدعوم في العقد المقفول ✓) ⇒ **إصلاح محلي مطلوب** ثم إعادة اختبار البوابة ✓

### ✅ عيب DOUBLE SUBMIT — **مُصلَح ومُثبَت** ✓
أُضيف حارس `submitting` في `submit()` (بنفس فلسفة `state.busy` في onToggle/onMove/onDelete ✓) + إعادة تعيين آمنة في كل مسارات الخروج ✓.
**الدليل (نقر ثلاثي متعمّد ×3 على كل عملية):**
| العملية | عدد الطلبات |
|---|---|
| CREATE | **1** `POST /slider/admin` (multipart) ✓ |
| SAVE/REMOVE (json) | **1** `PATCH /slider/admin/16` (json) ✓ |
| DELETE | **1** `DELETE /slider/admin/15` ✓ |
⇒ لا صفوف/أصول مكرّرة ✓ · لا 404 من DELETE ثانٍ ✓ · لا تبقّي النموذج عالقًا ✓

### ~~عيب محلي ثانٍ — DOUBLE SUBMIT~~
كل عملية حفظ أرسلت **طلبات مكرّرة متطابقة**: `[PATCH, PATCH]` عند ADD (multipart ×2 ✗) و`[PATCH, PATCH]` عند KEEP (json ×2 ✗) و`[PATCH ×3]` عند REMOVE ✗ ⇒ **معالج الحفظ غير محمي بحارس ازدواج** ✗ (بخلاف `onToggle`/`onMove`/`onDelete` المحميّة ✓) ⇒ يخصّ بوابة **B2** ⇒ **إصلاح محلي مطلوب** ✓ ثم إعادة اختبار B2 ✓

### B1 VALIDATION — نتائج جزئية ✓ + عيب محلي مثبَت ✗ (تحتاج إكمالًا)

| # | الحالة | التصنيف | الرسالة العربية الفعلية | Mutations | الحكم |
|---|---|---|---|---|---|
| 1 | **صورة تالفة** | رسالة **client** ✓ **لكن الطلب أُرسل** ✗ | «تعذّرت قراءة الصورة — تأكد أن الملف صورة صالحة.» ✓ | POST واحد ⇒ **فشل من الخادم** ✓ | ⚠️ **عيب محلي** ✗ |
| 2 | **>8MB** | رسالة **client** ✓ **لكن الطلب أُرسل** ✗ | «حجم الملف يتجاوز 8MB.» ✓ | POST واحد ⇒ **فشل** ✓ | ⚠️ **عيب محلي** ✗ |
| 3 | **altText > 300** | **client-side ✓✓** | «النص البديل يجب أن يكون بين 2 و300 حرف.» ✓ | **0** ✓✓ | ✅ **PASS** |
| 4 | رابط غير صالح `http://` | غير محسوم ✗ | «لا رسالة» ✗ | POST قيد التنفيذ ✗ | ⏳ يحتاج إعادة |
| 5 | `javascript:` | غير محسوم ✗ | «لا رسالة» ✗ | 0 ✗ | ⏳ يحتاج إعادة |
| 6 | `data:` | غير محسوم ✗ | «لا رسالة» ✗ | POST قيد التنفيذ ✗ | ⏳ يحتاج إعادة |
| 7 | `file:` | **لم يُنفَّذ** ✗ | — | — | ⏳ يحتاج إعادة |

**تحقّق عام في كل الحالات ✓:** لا `[object Object]` ✓ · لا stack trace ✓ · لا مسار فيزيائي ✓ · النموذج يبقى مفتوحًا وقابلًا للتصحيح ✓ · **لا صف/أصل QA ولا TEMP** ✓ (الطلبات المعلّقة لم تُنشئ صفوفًا ✓).

#### 🐞 العيب المحلي المكتشف (يحتاج إصلاحًا ثم إعادة البوابة)
في مسار `change` لحقل الصورة: عند صورة تالفة/كبيرة **تُعرض الرسالة في الصندوق ✓ لكن لا يُحفظ خطأ في `dMeta`** ✗ (يُرجَع `null` ✓) ⇒ حارس `submit()` `if (dMeta && dMeta.error)` **لا يلتقطها** ✗ ⇒ يُرسل طلب POST بلا داعٍ ✗ (يرفضه الخادم ✓ فالنتيجة آمنة ✓ لكنه **طلب غير ضروري** ✗ وهو مطلوب منعه client-side ✓).
**السبب الجذري الدقيق (من الكود ✓):** `fileMeta()` **يُرجع فعلًا** `{error:'…'}` ✓ (أسطر 213/214/226 ✓) — لكن معالج `change` كان **يُصفّر** `dMeta = null` / `mMeta = null` ✗ في مسار الخطأ (السطران 327/343 ✓) ⇒ حارس `submit()` لا يرى الخطأ ✗ ⇒ يُرسل POST ✗.
**✅ الإصلاح المُطبَّق (محلي، مُختبَر نحويًا ✓):** إبقاء الخطأ بدل تصفيره ⇒ `dMeta = meta` / `mMeta = meta` ✓ (`node --check` OK ✓ · md5 `9f33c770` ✓) ⇒ الآن تُرفض الصورة التالفة/>8MB **client-side بلا أي طلب** ✓ — **يحتاج إعادة اختبار الحالتين 1 و2** ⏳

### ✅ B1 VALIDATION = COMPLETE ✓ (بعد الإصلاح الخامس)

| # | الحالة | التصنيف | الرسالة العربية الفعلية | Mutations | الحكم |
|---|---|---|---|---|---|
| 1 | **صورة تالفة** | **client-side ✓✓** | «تعذّرت قراءة الصورة — تأكد أن الملف صورة صالحة.» ✓ | **0** ✓✓ | ✅ PASS |
| 2 | **>8MB** | **client-side ✓✓** | «حجم الملف يتجاوز 8MB.» ✓ | **0** ✓✓ | ✅ PASS |
| 3 | **altText > 300** | **client-side ✓✓** | «النص البديل يجب أن يكون بين 2 و300 حرف.» ✓ | **0** ✓✓ | ✅ PASS |
| 4 | **`http://`** | **server-side ✓** | «رابط غير صالح: المسموح مسار داخلي (#/...) أو http(s)://» ✓ | POST ⇒ **400** ✓ | ✅ PASS |
| 5 | **`javascript:alert(1)`** | **server-side ✓** | «رابط غير مسموح: سكيم خطر» ✓ | POST ⇒ **400** ✓ | ✅ PASS |
| 6 | **`data:text/html,…`** | **server-side ✓** | «رابط غير مسموح: سكيم خطر» ✓ | POST ⇒ **400** ✓ | ✅ PASS |
| 7 | **`file:///etc/passwd`** | **server-side ✓** | «رابط غير مسموح: سكيم خطر» ✓ | POST ⇒ **400** ✓ | ✅ PASS |

**في كل الحالات ✓:** لا `[object Object]` ✓ · لا stack trace ✓ · لا مسار فيزيائي ✓ · النموذج يبقى مفتوحًا وقابلًا للتصحيح ✓ · **طلب واحد لكل محاولة** ✓ (الحارس فعّال ✓) · **لا صف/أصل QA ولا TEMP** ✓
**دليل الإصلاح الخامس ✓:** الحالتان 1 و2 صارتا **0 mutations** ✓ (كانتا تُرسلان POST قبل الإصلاح ✗) ⇒ الرِّفض صار client-side كاملًا ✓

### ✅ B3 ERROR UX = COMPLETE ✓ (بتقنية التقييمات القصيرة ✓)

**الطريقة:** لكل حالة 4 تقييمات JS قصيرة (<5s ✓) + انتظارات خارجية — محاكاة آمنة على `ALW.api.post` فقط (بلا أي تعديل backend/nginx ✓).

| الحالة | الرسالة الفعلية للمستخدم | busy تحرر | retry | الطلبات | raw JSON | [object Object] | stack | path |
|---|---|---|---|---|---|---|---|---|
| **400** | «تعذّر التنفيذ» ✓ | ✓ (`busy=false` · زر الحفظ مفعّل ✓) | ✓ | **1** ✓ | ✗ | ✗ | ✗ | ✗ |
| **401** | «تعذّر التنفيذ» ✓ | ✓ | ✓ | **1** ✓ | ✗ | ✗ | ✗ | ✗ |
| **403** | «تعذّر التنفيذ» ✓ | ✓ | ✓ | **1** ✓ | ✗ | ✗ | ✗ | ✗ |
| **404** | «تعذّر التنفيذ» ✓ | ✓ | ✓ | **1** ✓ | ✗ | ✗ | ✗ | ✗ |
| **413** | «تعذّر التنفيذ» ✓ (محاكاة ✓ — **لا تغيير nginx** ✗) | ✓ | ✓ | **1** ✓ | ✗ | ✗ | ✗ | ✗ |
| **500 (5xx)** | «تعذّر التنفيذ» ✓ | ✓ | ✓ | **1** ✓ | ✗ | ✗ | ✗ | ✗ |
| **Network failure** | «تعذّر التنفيذ» + `Failed to fetch` ✓ | ✓ | ✓ | **1** ✓ | ✗ | ✗ | ✗ | ✗ |

**في كل حالة ✓:** الصفحة **مستقرة** ✓ (النموذج يبقى مفتوحًا ✓) · حقول النموذج **قابلة للتحرير** ✓ · **لا طلبات مكرّرة** ✓ (حارس الازدواج ✓) · لا انهيار UI ✓

**بعد الحالات ✓ (إلزامي):**
1. **إزالة كل المحاكاات** ✓ ⇒ `restored: true` ✓ (`ALW.api.post` عاد للأصل ✓)
2. **طلب حقيقي ناجح** ✓ ⇒ إنشاء شريحة فعلي عبر الواجهة ✓ ⇒ الخادم: **`id=17`** («QA-B3» ✓)
3. **تنظيف رسمي** ✓ ⇒ `DELETE /slider/admin/17` ⇒ **200** ✓
4. **تحقق نهائي** ✓ ⇒ `slider_slides=1` (id=9 ✓) · `uploads/slider=1` ✓ · TEMP=0 ✓ · `GET /slider`=200 ✓ · الخدمات 200 ✓

**ملاحظة دقّة:** الرسالة الظاهرة للنموذج = `error.message` القادمة من طبقة الـAPI ✓ — ومع أخطاء الـAPI الحقيقية تكون **عربية** ✓ (أُثبت في B1: «رابط غير مسموح: سكيم خطر» ✓)؛ وعند غياب رسالة من الخادم يظهر البديل العربي «تعذّر التنفيذ» ✓.

### B3 (محاولات سابقة) ⛔ TOOLING BLOCKER ✗
تنفيذ B3 **داخل استدعاء واحد** ✓ نجح في: الدخول ✓ · `#/slider` ✓ · `cards=1` ✓ — ثم **الحالات السبع كلها** أعادت:
```
Runtime.evaluate timed out; expression: (async (status) => { … }
```
⇒ **حدّ الأداة لتقييم JS = 5 ثوانٍ** ✗ بينما كل حالة تحتاج ~7.5s انتظار داخلي (فتح النموذج + زرع الملف + الحفظ + قراءة النتيجة) ✗ ⇒ **الأداة لا تستطيع تنفيذ حالة B3 الواحدة** ✗.
**السلامة ✓:** المحاكاة كانت مثبّتة **قبل** النقر على الحفظ ✓ ⇒ كل POST مُنع ✓ ⇒ **صفر mutations · صفر صفوف · صفر أصول** ✓. أثر وحيد عابر: استعادة `ALW.api.post` لم تُنفَّذ في تبويب QA ✗ (حالة صفحة فقط ✓ تُزول بإعادة التحميل ✓ — **لا تغيير في كود المنتج** ✗).
**الحل المُقترح للمحاولة القادمة:** تقسيم كل حالة إلى **خطوتين قصيرتين (<5s لكل تقييم)**: (1) فتح+زرع+تثبيت المحاكاة+نقر، (2) قراءة النتيجة ⇒ أو استخدام **زرع مباشر** (`ALW.api.post` مع `setTimeout` قصير) مع تقليل الانتظار إلى ~2.5s. **لا تعديل منتج بسبب عطل الأداة** ✗.

### B3 (محاولة سابقة) ⛔ TOOLING BLOCKER ✗ (لم تُنفَّذ)
محاولة تشغيل B3 **لم تبدأ** ✗: جلسة المتصفح **فُقدت بين الاستدعاءات** ✗ ⇒ `ReferenceError: ALW is not defined` ✓ و`الصفحة: False` ✓ قبل تثبيت أي محاكاة ✗.
⇒ **صفر اختبارات · صفر mocks مثبّتة · صفر mutations · صفر تغييرات في المنتج** ✓ (لم أُعدّل أي كود بسبب عطل الأداة ✓).
**الحالة بعدها (تحقّق فعلي):** `slider_slides=1` (id=9 ✓) · `uploads/slider=1` ✓ · TEMP=0 ✓ · الخدمات 200 ✓ — **نظيفة** ✓
**المطلوب في المحاولة القادمة (نفس الاستراتيجية):** محاكاة آمنة على طبقة `ALW.api` فقط (استبدال `post/patch/del` بأخطاء `{status}` ✓ + `TypeError('Failed to fetch')` للشبكة ✓) ⇒ تسجيل: الرسالة الظاهرة ✓ · ثبات الصفحة ✓ · تحرّر `busy` ✓ · إمكانية إعادة المحاولة ✓ · عدد الطلبات ✓ — ثم **إزالة المحاكاة كاملة** ✓ + **طلب حقيقي ناجح** ✓. تنبيه: جلسة اللوحة تفقد حالتها عند إعادة إنشاء التبويب ⇒ يجب تنفيذ **المرحلة كاملة في استدعاء واحد** ✓.

### B1 (محاولة سابقة) ✗ (عائق أداة)
محاولة إعادة الحالات 1 · 2 · 4-7 **لم تُنفَّذ** ✗ — أداة المتصفح تعذّرت قبل أي اختبار: `Runtime.evaluate timed out after 5s waiting for the daemon` ✗ عند أول خطوة تنقّل ⇒ **صفر اختبارات وصفر طلبات** ✓ (لا أثر على الحالة ✓).
**الحالة بعدها (تحقّق فعلي):** `slider_slides=1` (id=9 فقط ✓) · `uploads/slider=1` ✓ · TEMP=0 ✓ · الخدمات 200 ✓ — **لا مخلفات** ✓
**المطلوب في المحاولة القادمة:** جلسة متصفح مستقرة + الحالات: تالف (متوقع: رسالة ✓ + **0 mutations** ✓ بعد الإصلاح الخامس) · >8MB (نفسه ✓) · `http://` (غير صالح فعليًا ✓) · `javascript:` · `data:` · `file:` مع تصنيف client/server ✓

### B1 VALIDATION — (المحاولة الأولى، تاريخي)
**تصحيح ذاتي:** المحاولة الأولى استخدمت استدعاء `js()` خاطئًا (بُني نص الدالة دون تمرير الوسائط) ⇒ الحالة المُختبرة فعليًا كانت **«بلا ملف»** ✗ لا التالف/>8MB ✗ ⇒ **النتائج غير صالحة** ✗ ولم تُنشأ أي mutation ✓.
**ما ثبت فعليًا (حالة فرعية صحيحة ✓):** إرسال بلا صورة سطح المكتب ⇒ رفض **client-side** ✓ برسالة عربية «صورة سطح المكتب مطلوبة.» ✓ · **صفر طلبات شبكة** ✓ · النموذج يبقى مفتوحًا وقابلًا للتصحيح ✓
**الخطوة التالية:** إعادة B1 بالاستدعاء الصحيح: `js(FUNC + "(args)")` ✓ — الحالات: تالف · >8MB · alt>300 · رابط غير صالح · `javascript:` · `data:` · `file:` مع تصنيف client/server ✓.

### 📌 ملاحظة QA (سبب إرباك سابق)
محدّد إجراء الجوال في النموذج هو **`#sl-mobile-mode`** ✓ (`name=mobileMode` · القيم `keep|replace|remove` ✓) — وليس `#f-mobileMode` ✗. الاختبار الأول أظهر «no-select» ✗ فبدا الأمر عيب منتج ✗ ثم ثبت أنه خطأ محدّد في سكربت QA ✓.

**ملاحظة تشغيلية (A3):** إنشاء الشريحة يستغرق >6s على هذا السيرفر (1 vCPU) ⇒ يجب انتظار ظهور البطاقة في الواجهة **قبل** نقر أزرار الترتيب، وإلا وقع النقر على قائمة قديمة.
**ملاحظة استرجاع:** بعد اختبار الترتيب، `id=9` صار `sortOrder=1` ⇒ أُعيد إلى **0** عبر **JSON PATCH رسمي** على الحقل `sortOrder` ✓ (عملية غير مدمّرة ✓) وتم التحقق ✓.

**ملاحظة QA مهمّة (لإعادة الاستخدام):** نقطة نهاية الدخول تقبل `{phone, password}` ✓ (وليس `{identifier, password}` ⇒ 400) ✓ — تُستخدم لتنظيف QA عبر **DELETE API** الرسمي ✓ (لا حذف ملفات مباشر ✗).

### BATCH B — المرونة والأمان
- **B1** VALIDATION: صورة تالفة · >8MB · `altText>300` · رابط غير صالح · `javascript:` · `data:` · `file:` — رسائل عربية بلا `[object Object]` ولا stack trace ولا مسار فيزيائي ✓
- **B2** DOUBLE SUBMIT: CREATE · EDIT/SAVE · DELETE ⇒ **طلب تعديل واحد منطقي** ✓ (خصوصًا **DELETE = 1** ✓ بعد إصلاح الحارس ✓) + لا صفوف/أصول مكرّرة ✓
- **B3** ERROR UX: 400 · 401 · 403 · 404 · 413 · 5xx · انقطاع الشبكة — عبر **اعتراض/محاكاة على مستوى المتصفح** ✓ ثم **إزالة كل الاعتراضات** ✓ + إثبات طلب حقيقي ناجح ✓
- **B4** PERMISSION UI (محاكاة آمنة بلا تغيير بيانات الصلاحيات): read-only · no-create · no-update · no-delete ✓

### BATCH C — الإتاحة والبصري
- **C1** KEYBOARD: Tab · Shift+Tab · focus ظاهر ✓ — وModal: focus داخل ✓ · focus trap ✓ · Escape ✓ · استعادة التركيز ✓ · خلفية غير قابلة للتنقل ✓ — + labels ✓ · أسماء أزرار ✓ · إعلان الحالات/الأخطاء ✓ · لا عناصر مخفية قابلة للتنقل ✓
- **C2** RESPONSIVE: 360 · 390 · 430 · 768 · 1024 · 1366 · 1920 — فائض أفقي 0 ✓ · RTL ✓ · البطاقات/الأزرار/المعاينة/النموذج قابلة للاستخدام ✓
- **C3** LIGHT/DARK: نص · حدود · focus · تحقق · بطاقات · Modal · أزرار ✓ — وصور السلايدر **بلا أي filter/recolor** ✓
- **C4** **9 لقطات**: 390 فاتح · 390 داكن · 1366 فاتح · 1366 داكن · نافذة الإنشاء/التعديل · معاينة سطح المكتب بالأبعاد والنسبة · حالة صورة الجوال · تحذير النسبة · خطأ تحقق ✓

---

## 6) التقنيات المُثبَتة للـQA (لإعادة الاستخدام)

1. **الجلسة:** إرسال `submit` حقيقي على النموذج + انتظار 6–7s ⇒ `#/dashboard` ثم `#/slider`
2. **إثبات نوع الطلب:** اعتراض `ALW.api.post/patch/del` لتسجيل `{m, p, kind: json|multipart}`
3. **زرع صور:** توليد على `canvas` ⇒ `DataTransfer` ⇒ `input.files` ⇒ حدث `change`
4. **التحقق:** دائمًا **حالة الخادم** بعد كل عملية (`ALW.api.get('/slider/admin')`) لا الـDOM وحده
5. **الحماية:** `id=9` لا تُستخدم في أي اختبار مدمّر ✓

---

## 7) حدود النطاق (SCOPE LOCK) — مُلتزَم بها

ممنوع ولم يُنفَّذ: STEP 5 ✗ · Storefront Slider ✗ · Home Hero ✗ · Header ✗ · Catalog ✗ · Migration ✗ · Schema ✗ · تغيير بيانات الصلاحيات ✗ · تغيير pipeline التخزين ✗ · تغيير عقد الـAPI ✗
مسموح ونُفِّذ: إصلاحات محلية مثبتة في واجهة السلايدر ✓ + إصلاح nginx بموافقة صريحة ✓
