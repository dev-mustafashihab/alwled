# PHASE 3 — STEP 3G · ACCESSIBILITY + FINAL RESPONSIVE QA

**النوع:** إتاحة + QA نهائي (لا إعادة تصميم) · **التاريخ:** 2026-09-18
**النطاق:** Products listing · Listing Product Cards · Desktop Filter Toolbar · Mobile Filter Sheet · Categories · Brands · Loading · Empty · Error
**الأدوات:** Playwright (Chromium) · قياسات DOM/تباين مكتوبة يدويًا · **axe غير متاح محليًا ⇒ لم يُثبَّت أي شيء** (§54)

---

## 1. FILES MODIFIED (5)

| الملف | قبل (md5) | بعد (md5) | الأسطر | السبب |
|---|---|---|---|---|
| `shop/assets/js/shop.pages.js` | `3b38eeed` | `94d5917f` | +166 / −14 | وحدة `filterA11y` (aria/قفل تمرير/تركيز/حبس/Escape/إرجاع) + عدّاد النتائج + haspopup + id العنوان |
| `shop/assets/js/shop.js` | `897310d7` | `a3a507d1` | +9 / −6 | `stateBlock` (role + أيقونة aria-hidden) · `loadingBlock` (هياكل aria-hidden + رسالة role=status) |
| `shop/assets/css/shop.css` | `b1a0c32f` | `cd220211` | +21 / −0 | `--cat-focus` (فاتح/داكن) + حلقة تركيز الكتالوج + مساحة نقر للمسار + زر إعادة المحاولة ≥44 |
| `shop/assets/css/shop.filters.css` | `bb772219` | `3e79222e` | +3 / −2 | لوحة مغلقة `visibility: hidden` (خارج ترتيب التركيز) |
| `shop/index.html` | `408f8429` | `daa29403` | +3 / −1 | **إزالة `aria-live="polite"` العام من `#shop-view`** (كان يعلن الصفحة كاملة) + تعليق |

**لم تُلمس:** `tokens.css` · `shop.home.css` · `shop.header.css` · `shop.search.css` · Backend/DB/API/Routes · `src/**` (0 ملفات).
**Backend files modified = 0** · **new `!important` = 0** (shop.css 5 كما كان · filters 0) · **صفر مكتبة/حزمة جديدة**.
**ملاحظة نطاق:** `index.html` ليس في قائمة الممنوع في §50 وليس في قائمة المتوقّع؛ التغيير سطر واحد (`aria-live`) + تعليق — وهو الشرط اللازم لمنع الإعلان المزدوج/الضجيج (§23). قابل للإرجاع بسطر واحد.

---

## 2. ACCESSIBILITY AUDIT TABLE (قبل → بعد)

| # | المشكلة | المكوّن | العرض | فاتح/داكن | لوحة المفاتيح | قارئ الشاشة | الخطورة | السبب الجذري | الملف/الدالة | إصلاح؟ |
|---|---|---|---|---|---|---|---|---|---|---|
| D1 | لا دور dialog/aria-modal/اسم | لوحة الفلاتر | ≤768 | كلاهما | — | يُقرأ كمحتوى عادي | عالية | لا سمات ARIA إطلاقًا | `shop.pages.js` (باني اللوحة) | **نعم** |
| D2 | لا قفل تمرير للخلفية | اللوحة | ≤768 | كلاهما | Tab يخرج للصفحة | — | عالية | لا lock (بخلاف القائمة/البحث) | `shop.pages.js` | **نعم** |
| D3 | لا تركيز ابتدائي | اللوحة | ≤768 | كلاهما | التركيز يبقى على زر خلف الـscrim | — | عالية | كود قديم يفعّل فقط عند `innerWidth>768` (زر مخفي) | `shop.pages.js` | **نعم** |
| D4 | لا حبس تركيز | اللوحة | ≤768 | كلاهما | Tab يهرب بعد 10 عناصر إلى البطاقات/الأكورديون/body | — | عالية | لا معالج Tab | `shop.pages.js` | **نعم** |
| D5 | Escape لا يغلق | اللوحة | ≤768 | كلاهما | Escape بلا أثر | — | عالية | لا معالج | `shop.pages.js` | **نعم** |
| D6 | لا إرجاع تركيز | اللوحة | ≤768 | كلاهما | بعد الإغلاق التركيز على زر مخفي/body | — | عالية | لا إرجاع | `shop.pages.js` | **نعم** |
| D7 | `aria-expanded` عالق true | زر الفلاتر | ≤768 | كلاهما | — | حالة خاطئة | متوسطة | الإغلاق عبر ✕/تم/scrim لا يحدّث السمة | `shop.pages.js` | **نعم** |
| D8 | عناصر اللوحة المغلقة قابلة للتبويب | اللوحة | ≤768 | كلاهما | 5 توقفات داخل لوحة مغلقة | — | عالية | `transform` فقط (العنصر ما زال مرئيًا) | `shop.filters.css` | **نعم** |
| D9 | حالة عالقة عند 768→769 | اللوحة | عبور 768/769 | كلاهما | — | aria-modal قد يبقى | عالية | لا معالج resize | `shop.pages.js` | **نعم** |
| D10 | تراكب طبقات (فلاتر+بحث) | اللوحة/البحث | ≤768 | كلاهما | — | — | منخفضة (غير قابل للوصول بالماوس: الـscrim يحجب الهيدر) | لا إغلاق متبادل | `shop.pages.js` | **نعم** |
| D11 | حلقة تركيز 2.83:1 | كل تركيز الكتالوج | ≤768/ديسكتوب | **داكن** | مؤشر ضعيف | — | عالية | `#1F5FBF` على `#121b2b` | `shop.css` | **نعم** |
| D12 | زر السلة: حلقة على الزر الأزرق | Product Card | كل | داكن (2.83) / فاتح سليم | — | — | = D11 | `outline-offset:2px` يضعها على البطاقة ⇒ المشكلة لون لا موضع | `shop.css` | **نعم (مع D11)** |
| D13 | `aria-live` عام على `#shop-view` | كل الصفحة | كل | كلاهما | — | يعلن الصفحة كاملة عند كل رسم | متوسطة | سمة عامة + عدّاد بلا دور | `index.html` + `shop.pages.js` | **نعم** |
| D14 | هياكل التحميل بلا `aria-hidden` | Loading | كل | كلاهما | لا أثر (0 توقف) | ضجيج نظري | منخفضة | لا سمات | `shop.js` | **نعم** |
| D15 | حالة الفراغ بلا دور + أيقونة تُقرأ | Empty | كل | كلاهما | — | «📦» تُقرأ + لا إعلان | متوسطة | لا role/aria-hidden | `shop.js` | **نعم** |
| D16 | حالة الخطأ بلا دور + زر 42px | Error | كل | كلاهما | — | لا إعلان فوري | متوسطة | لا role/aria-hidden + 42<44 | `shop.js` + `shop.css` | **نعم** |
| D17 | رابط المسار 42×21 | Breadcrumb | ≤430 | كلاهما | هدف لمس صغير | — | منخفضة | مساحة النقر = حجم النص | `shop.css` | **نعم (مساحة نقر)** |
| D18 | أسماء العناصر | 25 عنصرًا تفاعليًا | 390 | كلاهما | — | — | **لا عيب** | — | — | لا |

**ملاحظات تدقيق (سليمة أصلًا، لم تُلمس):** الهيدر (زر بحث بـ`aria-haspopup="dialog"` و`aria-controls`) · القائمة (Escape + قفل + إرجاع تركيز إلى ☰) · البحث (Escape + قفل + إرجاع إلى زر البحث) · الشرائح/الحقول (كل الحقول الثمانية لها اسم متاح: `aria-label` أو `label[for]` أو ملفوفة في `<label>`) · الشرائح `input[type=checkbox]` أصلية داخل `<label>` (لا ARIA مخصّص) · `#shop-toolbar` بـ`role="search"` · مسار التنقّل/الصفحات.

---

## 3. MOBILE FILTER SHEET — BASELINE (قبل، مُقاس)

| البند | قبل |
|---|---|
| role / aria-modal / aria-labelledby | `null / null / null` (مغلق ومفتوح) |
| `visibility` مغلق | `visible` (قابل للتركيز) |
| `aria-expanded` | `false` عند الفتح→`true` ✓ · **يبقى `true`** بعد الإغلاق بـ✕/تم/scrim ✗ |
| `aria-controls` | `shop-toolbar` ✓ (موجود ✓) · `aria-haspopup` = null ✗ |
| التركيز بعد الفتح | يبقى على زر الفلاتر (خلف الـscrim) ✗ |
| حبس التركيز | 20 Tab: 10 داخل اللوحة ثم يهرب إلى البطاقات/الأكورديون/body ✗ · Shift+Tab يهرب أيضًا ✗ |
| Escape | لا يغلق (`before == after`) ✗ |
| التركيز بعد الإغلاق | ✕ ⇒ على زر الإغلاق المخفي · scrim ⇒ body ✗ |
| قفل التمرير | `body/html = visible` (الصفحة تتحرك خلف اللوحة) ✗ |
| الإغلاق بالخلفية (scrim) | **موجود أصلًا** (`onclick`) ⇒ حُوفظ عليه ✓ |
| 768→769 واللوحة مفتوحة | `display: contents` ✓ · scrim `display:none` ✓ · لكن `is-open` و`aria-expanded=true` يبقيان ✗ |
| تراكب | ممكن برمجيًا (فلاتر+بحث معًا) ✗ |
| قفل تمرير عالق | لا (لا قفل أصلًا) |

---

## 4–11. النتائج بعد الإصلاح (مُقاسة)

**4) Dialog semantics:** مغلق ⇒ لا سمات ✓ · مفتوح (≤768) ⇒ `role="dialog"` + `aria-modal="true"` + `aria-labelledby="shop-filters-title"` (يشير إلى `<strong>الفلاتر والترتيب</strong>` الموجود) ✓ · على ≥769 لا دور (المحتوى inline عادي) ✓ · لا ARIA مكرّر/متعارض ✓.
**5) Opener:** `aria-expanded` false↔true متزامن في كل المسارات (فتح/✕/تم/scrim/Escape/resize/تغيير مسار) ✓ · `aria-controls="shop-toolbar"` (موجود فعلًا) ✓ · أُضيف `aria-haspopup="dialog"` ✓.
**6) Initial focus:** الفتح ⇒ التركيز على **زر الإغلاق** (أول عنصر تفاعلي في اللوحة) ✓ — نُفِّذ بعد أن تصبح اللوحة مرئية (انتقال `visibility`) وإلا يفشل التركيز.
**7) Focus trap:** 20 Tab ⇒ **20/20 داخل اللوحة** (دورة: إغلاق → بحث → تصنيف → علامة → ترتيب → من → إلى → متوفر → عروض → تفريغ → عرض النتائج → إغلاق…) ✓ · Shift+Tab ⇒ داخل اللوحة ✓ · **لا حبس على الديسكتوب** (المعالج يتحقق من `≤768` + الحالة مفتوحة) ✓.
**8) Escape:** يغلق ✓ + إرجاع التركيز إلى زر الفلاتر ✓ + تحرير التمرير ✓ · `stopPropagation` ⇒ لا تداخل مع Escape الخاص بالهيدر/القائمة/البحث ✓.
**9) Focus restoration:** ✕ ⇒ الزر ✓ · «عرض النتائج» ⇒ الزر ✓ · scrim ⇒ الزر ✓ · «تفريغ الفلاتر» (ينقّل) ⇒ الزر الجديد بعد إعادة الرسم ✓.
**10) Backdrop:** سلوك النقر على الخلفية **الموجود** محفوظ كما هو (لم يُخترع جديد) + إرجاع تركيز منطقي ✓.
**11) Body scroll lock:** مفتوح ⇒ `html+body = hidden` ✓ · مغلق بكل المسارات ⇒ `visible` ✓ · **لا قفل عالق** (اختبار 768→769 ثم العودة، وتغيير المسار، وكل التصادمات) ✓ — بنفس آلية القائمة/البحث (حفظ/استرجاع `style.overflow`).

**12) 768→769 (واللوحة مفتوحة):** `is-open=false` · `aria-expanded=false` · الدور/`aria-modal` مُزالان · `body/html = visible` · scrim مخفي · شريط الأدوات inline مرئي ✓ — ثم العودة إلى 768: لا حالة عالقة ✓.
**13) العناصر المخفية:** اللوحة المغلقة `visibility: hidden` ⇒ **0 عنصر من اللوحة في ترتيب التركيز** (كان 5) ✓ · استراتيجية `display: contents` من 3B كما هي (0 تحكم مكرّر · 0 ID مكرّر) ✓ · على ≥769 التحكمات كلها قابلة للتبويب عادة ✓.
**14) ترتيب Tab:** 390: skip → الشعار → بحث → السلة → ☰ → مسار → **زر الفلاتر → [اللوحة المفتوحة فقط]** → البطاقات (رابط الصورة → الاسم → زر السلة) ✓ · 1366: skip → الشعار → 4 روابط تنقّل → حقل بحث الهيدر → دخول → السلة → المسار → **الشريط inline (8 تحكمات)** → البطاقات ✓ — لا قفزات إلى UI مخفي ✓.
**15) Product Card keyboard:** `.shop-card__details` = `display: none` ⇒ **ليس في ترتيب التركيز** ✓ (لا توقف زائد) · رابط الصورة + رابط الاسم + زر السلة قابلة للوصول مستقلًا ✓ (3 توقفات/بطاقة، لا تكرار).
**16) Stretched link:** `::after` لا يحجب زر السلة (نقره يعمل ✓) ولا الروابط ✓ ولا يمنع التحديد بشكل غير معقول ✓ — لا إعادة تصميم.

---

## 17–20. التركيز · اللمس · التسميات

**17) Focus contrast (مقاس على الخلفية المجاورة الفعلية):**

| العنصر | فاتح قبل/بعد | داكن قبل → بعد |
|---|---|---|
| زر الفلاتر | 5.82 / **5.82** | 3.08 → **3.63** |
| زر الإغلاق · البحث · التصنيف · العلامة · الترتيب · من · إلى · متوفر · عروض · تفريغ | 6.09 / **6.09** | **2.83 → 3.34** |
| رابط صورة البطاقة / الاسم / زر السلة | 5.82–6.09 / **6.09** | **2.92/2.83/1 → 3.34** |
| رابط المسار | 5.82 / **5.82** | **→ 3.63** |

الإصلاح: متغيّر محلي `--cat-focus` داخل `#shop-view` فقط — فاتح `--store-primary` (بلا تغيير) · داكن `--store-info` (#2563eb) = **3.34–3.63 ≥ 3:1** ✓. **لم يُغيَّر `--store-primary` عالميًا · لا teal · لا حلقتان** (حلقة واحدة `2px solid` + `offset 2px`) ✓. **الهيدر/القائمة/البحث خارج `#shop-view` ⇒ لم تُلمس** (ملاحظة: القاعدة العامة `.shop-body :focus-visible` لا تزال 2.83 في الداكن خارج الكتالوج — **مؤجّل ومُوثّق §55**).
**18) Focus consistency:** لغة واحدة: `2px solid` + `offset 2px` + `box-shadow:none` لكل: زر الفلاتر · الإغلاق · الحقول · القوائم · الشرائح · روابط البطاقة · زر السلة · بطاقات التصنيف · زر إعادة المحاولة ✓ — تحقّق بلوحة المفاتيح (Tab) لا `focus()` برمجي: كلها تُظهر الحلقة ✓.
**19) Touch targets (360/390/430):** كل عناصر الكتالوج ≥44 إلا **رابط المسار 42×21** ⇒ أُضيفت مساحة نقر `::after { inset: -6px 0 }` = **42×33** (نقطة 4px فوق الرابط تُصيب الرابط ✓) بلا أي تغيير بصري ✓ · زر إعادة المحاولة 42 → **44** ✓ · صفوف الشرائح ≥44 ✓ · زر الفلاتر 44 ✓ · الإغلاق 44×44 ✓ · زر السلة 44 ✓ · بطاقات التصنيف 92 ✓ · ترقيم الصفحات: غير موجود بالبيانات الحالية ✓.
**20) Form labels:** الثمانية كلها بأسماء متاحة ✓ (بحث/تصنيف/علامة/ترتيب: `aria-label` + `label[for]`؛ من/إلى: `aria-label` + placeholder «من/إلى»؛ متوفر/عروض: `<label>` ملفوف) ✓ — `placeholder` ليس بديلًا في أي حالة ✓ · لا تكرار تسميات ✓.
**21) Checkbox:** `input[type=checkbox]` أصلية داخل `<label>` ✓ · النقر على النص/الصف يبدّل ✓ (مقاس: instock false→true) · **Space يبدّل** ✓ (مقاس: false→true) · لا ARIA مخصّص ✓.
**22) Price inputs:** `type="number"` + `inputmode="numeric"` + `min="0"` + `step="1"` كما هي ✓ · أسماء متاحة ✓ · **لا تحقق جديد** ✓ · سلوك 3C (min>max ⇒ فراغ صادق) كما هو ✓.

---

## 23–26. الإعلانات والحالات

**23) Result announcement:** أُزيل `aria-live` العام عن `#shop-view` وأصبح العدّاد `role="status" aria-live="polite" aria-atomic="true"` ⇒ إعلان مختصر («2 منتج») عند تغيّر النتائج فقط ✓ — لا إعلان لكل ضغطة مفتاح (البحث داخل الأدوات مُهيّأ بتأخير أصلي) ✓ — لا تكرار مع إعلانات بحث الهيدر (منطقة مستقلة) ✓.
**24) Loading:** `aria-busy="true"` على القسم ✓ · كل الهياكل + شبكتها `aria-hidden="true"` ✓ · **0 توقف تركيز** داخل الهياكل ✓ · رسالة «جارٍ تحميل المنتجات…» `role="status"` ✓ — **لم تُعدَّل بنية الهياكل** (التصميم §35).
**25) Empty:** «لا توجد منتجات مطابقة» `role="status"` ✓ + أيقونة `aria-hidden` ✓ + بلا إعلان متكرّر (لا live عام) ✓ + لا تحكم ميت (لا زر) ✓ + المسار المنطقي (العدّاد «0 منتج» مُعلن ✓).
**26) Error:** `role="alert"` ✓ + أيقونة `aria-hidden` ✓ + زر «إعادة المحاولة» باسم متاح ✓ + تركيز مرئي ✓ + **44px** ✓ + يعمل كما قبل (0 تغيير منطق) ✓.

**27) Categories/Brands a11y:** كل بطاقة = رابط واحد ✓ باسم متاح (النص + العدّاد) ✓ · العدّاد لا يكرّر بشكل مربك («2 منتج» مرة واحدة) ✓ · تركيز مرئي ✓ · اسم طويل (3E line-clamp) ✓ · عدّاد صفر ظاهر ✓ — **التخطيط 3E كما هو** ✓.
**28) RTL:** `direction: rtl` ✓ · ترتيب Tab = ترتيب DOM المنطقي (لا انعكاس) ✓ · لا قصّ نصوص ✓ · لا تراكب شارات ✓ · ترتيب السعر (الآن ثم القديم ثم الخصم) ✓ · محاذاة الحقول ✓ · موضع زر الإغلاق (أعلى اللوحة) ✓ · لا ترقيم صفحات حاليًا ✓ — كل الإصلاحات بخصائص منطقية (`inset`, `inset-inline`, `inset: -6px 0`) ✓.
**29) Text zoom (تقريب 200% بجذر 200%):** 390: فائض 0 · البطاقة 327 · زر السلة 44 · **لا قصّ** (0 عناصر `scrollWidth > clientWidth`) ✓ · 1366: فائض 0 · البطاقة 357 · زر 44 · لا قصّ ✓ — ملاحظة: Playwright لا يوفر text-only zoom أصلي ⇒ استُخدم تكبير جذر 200% (أقرب بديل) ✓.
**30) Browser zoom:** تكبير 200% فعلي = عرض CSS نصفي (780→390) ⇒ **مغطّى بقياسات 390/683** ✓ · لا فائض أفقي ناتج عن الكتالوج في أي عرض ✓.
**31) Reduced motion:** `prefers-reduced-motion: reduce` ⇒ انتقال اللوحة/الـscrim `1e-06s` (معطّل) ✓ · انتقال البطاقة 0s ✓ · بطاقة التصنيف 0s ✓ · بلا shimmer للهياكل ✓ · لا حركة تابعة للهيدر/الرئيسية أُلغيت (خارج النطاق) ✓.
**32) Non-color cues:** التركيز = حلقة **+** إزاحة ✓ · الحالة المختارة/المرشّحة = حدّ + خلفية + نص ✓ · الخصم = شارة نصية «-16.67%» + سعر قديم مشطوب **+** «وفّر» (مخفية في القوائم لأن الشارة نصية كافية) ✓ · الخطأ = نص + أيقونة + زر ✓.
**33) Light/Dark:** كلا الوضعين مُقاسان: التركيز 6.09 (فاتح) / 3.34 (داكن) ✓ · نص باهت 4.76/6.73 (3D) ✓ · السعر القديم 4.76/6.73 ✓ · تسميات الفلاتر ✓ · الشرائح ✓ · الفراغ/الخطأ ✓ · عدّادات التصنيف ✓ · CTA ✓ — **لم يُعَد فتح تصميم 3F** ✓.

---

## 34. RESPONSIVE MATRIX (مُقاس، بعد)

| العرض | الهيدر | الشريط | الفلاتر | أعمدة الشبكة | بطاقة المنتج | الصورة | فائض أفقي | قفل الجسم | حلقة التركيز |
|---|---|---|---|---|---|---|---|---|---|
| 360 | 65 | inline | sheet | 2 | 162×327 | 160×140 | 0 | مفتوح | 6.09 |
| 390 | 65 | inline | sheet | 2 | 177×327 | 175×140 | 0 | مفتوح | 6.09 |
| 430 | 65 | inline | sheet | 2 | 197×306 | 195×140 | 0 | مفتوح | 6.09 |
| 600 | 65 | inline | sheet | 2 | 282×306 | 280×140 | 0 | مفتوح | 6.09 |
| 640 | 65 | inline | sheet | 2 | 302×306 | 300×140 | 0 | مفتوح | 6.09 |
| 641 | 65 | inline | sheet | 2 | 299×325 | 297×156 | 0 | مفتوح | 6.09 |
| 768 | 65 | inline | sheet | 3 | 237×325 | 235×156 | 0 | مفتوح | 6.09 |
| 769 | 65 | inline | inline | 3 | 238×325 | 236×156 | 0 | مفتوح | 6.09 |
| 900 | 65 | inline | inline | 3 | 281×325 | 279×156 | 0 | مفتوح | 6.09 |
| 1023 | 65 | inline | inline | 3 | 320×325 | 318×156 | 0 | مفتوح | 6.09 |
| 1024 | 74 | inline | inline | 3 | 320×357 | 318×188 | 0 | مفتوح | 6.09 |
| 1280 | 74 | inline | inline | 4 | 280×357 | 278×188 | 0 | مفتوح | 6.09 |
| 1366 | 74 | inline | inline | 4 | 280×357 | 278×188 | 0 | مفتوح | 6.09 |
| 1440 | 74 | inline | inline | 4 | 280×357 | 278×188 | 0 | مفتوح | 6.09 |
| 1920 | 74 | inline | inline | 4 | 280×357 | 278×188 | 0 | مفتوح | 6.09 |

**تصنيفات/علامات:** 390 ⇒ 366×92 · عمود واحد · 1366 ⇒ 276×96 · 4 أعمدة ✓ (3E مطابق) · ترقيم الصفحات: **غير موجود** (بيانات المتجر منتجان ⇒ `totalPages=1`) ⇒ لا قياس.

---

## 35. LOADING SKELETON — DECISION

`loadingBlock` مشترك بين **13 مستهلكًا**: المنتجات · الرئيسية · التصنيفات · العلامات · تفاصيل المنتج · الطلبات · تفاصيل الطلب · الإشعارات · السلة · الدفع · الـcheckout · التوثيق · الحساب.
**القرار: DEFER** — لا حل CSS-scoped ممكن: كتل التحميل للمنتجات/التصنيفات/تفاصيل المنتج كلها أبناء مباشرون لـ`#shop-view` بنفس البنية (`.shop-section[aria-busy] > .shop-grid > .skeleton--card`) بلا أي صنف مميّز ⇒ أي قاعدة CSS ستصيب الصفحات الأخرى. يتطلب تمييزًا في الماركب/JS (تعديل JS مشترك) ⇒ **لم يُعدَّل** ✓ (مطابق لتعليمات §35).

---

## 36–38. FUNCTIONAL REGRESSION · inStock · offers

**36) 3C — مطابق تمامًا:**

| الحالة | البطاقات | العدّاد | الطلبات |
|---|---|---|---|
| base | 2 | 2 منتج | (من الرسم الأول) |
| minPrice=400 | 1 | 1 منتج | `products?page=1&limit=12&minPrice=400` + categories + brands |
| maxPrice=400 | 1 | 1 منتج | `…&maxPrice=400` |
| minPrice=99999 | 0 | 0 منتج | `…&minPrice=99999` |
| min=300&max=600 | 2 | 2 منتج | `…&minPrice=300&maxPrice=600` |
| categoryId=293 | 2 | 2 منتج | `…&categoryId=293` |
| brandId=292 | 0 | 0 منتج | `…&brandId=292` |
| sortBy=price&asc | 2 | 2 منتج | `…&sortBy=price&sortOrder=asc` |
| offers=1 | 1 | 1 منتج | **بلا معامل offers** (فلترة محلية على الصفحة) |
| page=2 | 2 | 2 منتج | `page=2` ثم تصحيح `page=1` (طلبان — سلوك 3C الموثّق) |

**37) inStock — STOP + تقرير (بلا إصلاح صامت):** أُعيد الفحص بعد 3G:
`#/products?inStock=1` ⇒ الـhash يحمل `inStock=1` لكن **الطلب المرسل لا يحمل `inStock`** (`products?page=1&limit=12` فقط) · النتائج **2 منتج** (بلا تغيير) · واجهة API الزائر لا تدعم `inStock` (3C: HTTP 400 «property should not exist»).
⇒ **المشكلة: مربّع «المتوفر فقط» معروض للمستخدم لكنه لا يفلتر فعليًا — ادعاء وظيفة غير موجودة.** لم أُعدّله ولم أحذفه (§37: قرار منتج). **الخيارات المطروحة للموافقة:** (أ) إخفاؤه حتى يدعم الـBackend الفلتر · (ب) تعطيله مع توضيح «قريبًا» · (ج) إبقاؤه كما هو. **بانتظار قرارك.**
**38) offers:** السلوك الحالي محفوظ (فلترة محلية على صفحة النتائج) · الطلب بلا معامل `offers` · الحالة المرئية/ARIA صحيحة (checkbox أصلي + مُعلن) ✓ · القيد موثّق (قد يكون `total` غير دقيق لو تجاوزت النتائج صفحة واحدة — بالبيانات الحالية منتجان ⇒ دقيق).

---

## 39–45. REGRESSION

**39) Home (مقفل):** 390 ⇒ بطاقة **177×321** ✓ (وسائط 133) · 768 ⇒ 237×491 · 1366 ⇒ 280×512 ✓ · الهيرو/التصنيفات/العلامات/الثقة/التذييل كما هي ✓ · فائض 0 ✓ · لا تعديل على أي ملف Home ✓.
**40) Header:** جوال **65px** ✓ · ديسكتوب **74px** ✓ · sticky كما هو ✓ · الشعار 34/36 كما هو ✓ · لا فائض ✓ · لا تعديل على `shop.header.css` ✓.
**41) Menu:** يفتح/يغلق ✓ · التركيز ينتقل إلى أول رابط ✓ · Escape يغلق ✓ · **قفل الجسم يُسترجع** ✓ · التركيز يعود إلى ☰ ✓ · تحكم الثيم يعمل (استُخدم لقياسات الداكن) ✓ · لا إعادة هيكلة ✓.
**42) Search:** `top = 65` ✓ · الصفوف 2 × **90px** ✓ · الصورة ≈68 (لا صور بالبيانات) ✓ · Escape يغلق + التركيز يعود لزر البحث ✓ · الإعلان «نتائج (2)» قائم ✓ · **تسريب CSS البطاقة = 0** (`cardLeak 0`) ✓ · لا إعادة تصميم ✓.
**43) Modal collisions:** فلاتر→قائمة: القائمة فقط مفتوحة · `body hidden` · الفلاتر مغلقة · `expanded=false` ✓ · قائمة→إغلاق: `body visible` + التركيز على ☰ ✓ · فلاتر→بحث: البحث فقط + `body hidden` ✓ · بحث→إغلاق: `body visible` + التركيز على زر البحث ✓ · **لا قفل عالق · لا scrim عالق · لا تراكب** ✓ (فتح البحث برمجيًا والفلاتر مفتوحة ⇒ الفلاتر تُغلق تلقائيًا ✓).
**44) Route change واللوحة مفتوحة:** `#/products?categoryId=293` ⇒ اللوحة مغلقة · `aria-expanded=false` · `body visible` · بلا scrim عالق ✓ (إعادة الرسم تُنظّف + `afterRender` يضمن التحرير) ✓.
**45) Product Card (3D مقفل):** 390 ⇒ **177×327** ✓ · 1024 ⇒ **320×357** ✓ · 1366 ⇒ **280×357** ✓ — **بلا أي تغيير** (لا تعديل هندسي في 3G؛ إصلاح التركيز لوني فقط) ✓.
**46) Taxonomy (3E مقفل):** 390 ⇒ **366×92** ✓ · 1366 ⇒ **276×96** ✓ — بلا تغيير ✓.

---

## 47–49. OVERFLOW · CONSOLE · NETWORK

**47) Overflow:** `scrollWidth == clientWidth` على **15 عرضًا** (0 فائض) ✓ · داخل اللوحة: منطقة تمرير عمودية واحدة `overflow-y: auto` (scrollH 779 / clientH 691) · `scrollWidth == clientWidth` (390) ✓ · **0 عنصر بتمرير أفقي متداخل** ✓.
**48) Console:** **0 errors · 0 warnings · 0 pageerror** في كل تشغيلات 3G (التدقيق/المصفوفة/اللقطات/الإضافية) ✓ — لا ID مكرّر · لا ARIA غير صالح · لا أخطاء تركيز/مستمعات ✓.
**49) Network:** لا طلب جديد من 3G ✓ · **فتح/إغلاق اللوحة = 0 طلب** ✓ · عدّ الطلبات لكل فلتر كما في 3C ✓ · لا طلب عند تغيير الحجم أو التركيز ✓.

---

## 50–53. JS/CSS CHANGES · SCOPE · TOOLING

**50) JS — سطرًا بسطر (`shop.pages.js`، كلها إتاحة، صفر منطق أعمال):**
1. وحدة `filterA11y` جديدة: `set(open,restore)` · `lock()` (حفظ/استرجاع `overflow`) · `applyModal()` (role/aria-modal/labelledby) · `focusables()` · `onKeydown` (Escape + Tab/Shift+Tab) · `onResize` (تنظيف 768→769) · `closeOthers()` · `afterRender()` (تزامن + إرجاع تركيز معلّق).
2. زر الفلاتر: `aria-haspopup="dialog"` + معالج نقرة → `filterA11y.set(!isOpen(), false)`.
3. عدّاد النتائج: `role="status" aria-live="polite" aria-atomic="true"`.
4. عنوان اللوحة `<strong id="shop-filters-title">`.
5. «عرض النتائج» → `set(false, true)` · «تفريغ الفلاتر» → `markPending()` + نفس التنقّل · scrim → `set(false, true)` (السلوك محفوظ) · ✕ → `set(false, true)`.
6. نهاية `renderProducts` → `setTimeout(filterA11y.afterRender, 0)`.
**`shop.js`:** `stateBlock` → `role = error ? 'alert' : 'status'` + أيقونة `aria-hidden` · `loadingBlock` → `aria-hidden` للهياكل + شبكتها + `role="status"` للرسالة.
**51) CSS — المقاطع:** `shop.css`: `#shop-view{--cat-focus}` + `[data-theme='dark'] #shop-view{--cat-focus:var(--store-info)}` + `#shop-view :focus-visible` + `#shop-view .shop-crumbs a{position:relative}` + `::after{inset:-6px 0}` + `#shop-view .state .btn{min-height:44px}` · `shop.filters.css`: `.shop-filters__sheet{visibility:hidden}` + `transition … , visibility .28s` + `.is-open … {visibility:visible}`.
**52) Backend files modified = 0** ✓ · **53) new `!important` = 0** ✓ · **54) Automated a11y tooling:** axe/axe-core **غير متاح** محليًا ولم يُثبَّت أي شيء (تعليمات §54) ⇒ استُخدمت فحوص DOM مكتوبة + اختبارات لوحة مفاتيح فعلية + حساب تباين WCAG. **لا ندّعي شهادة WCAG.**

---

## 55–56. SCREENSHOTS · DEFERRED

**56) اللقطات (33 في `tools/qa/stage3g/shots/` + 9 لقطات تركيز بلوحة المفاتيح `kf-*`):**
Products: 360L · 390L · 390D · 768L · 769L · 1024L · 1024D · 1366L · 1366D · 1920L ·
Filters open: 360L · 390L · 390D · 768L ·
Focus (لوحة مفاتيح): 390 opener · 390 close · 390 checkbox · 390 add · 390 card-link · 390 taxonomy · 1366 card-link · 1366 taxonomy · 1366 retry ·
Regression: Home 390/1366 · Menu 390 · Search 390 · Categories 390/1366 · Brands 390/1366 · Empty dark · Error dark.
**55) مؤجّل/ملاحظات:**
1. **inStock UX** — بانتظار قرارك (§37، قرار منتج لا إصلاح إتاحة).
2. حلقة التركيز العامة خارج `#shop-view` (هيدر/قائمة/بحث/صفحات أخرى) لا تزال **2.83:1 في الداكن** — خارج نطاق 3G؛ الإصلاح سطر واحد (استخدام `--cat-focus` في القاعدة العامة) متى أذنت.
3. هياكل التحميل لا تطابق شكل بطاقة 3D (مشترك 13 صفحة) ⇒ DEFER.
4. `offers` فلترة محلية على الصفحة (قيد 3C، لا يُحل هنا).
5. ترقيم الصفحات غير قابل للاختبار (منتجان) · حالة «غير متوفر» غير موجودة بالبيانات · لا صور منتجات (قياس `object-fit` غير ممكن بالبيانات).
6. `aria-expanded` عند 768→769 يُصفَّر مع إغلاق اللوحة (لا سمة عالقة على زر مخفي) ✓.
7. الهياكل/الترقيم في `role=search` للشريط: سليم كما هو.

---

## 57. BEFORE / AFTER — كل إصلاح فعلي

| # | قبل | السبب الجذري | الإصلاح | بعد | ملفات | محددات/دوال |
|---|---|---|---|---|---|---|
| 1 | لا دور/aria-modal | لا سمات | إضافة `role/aria-modal/aria-labelledby` عند الفتح (≤768 فقط) | dialog صحيح + اسم | `shop.pages.js` | `filterA11y.applyModal` |
| 2 | الصفحة تتحرك خلف اللوحة | لا lock | حفظ/ضبط `html+body.overflow='hidden'` | مفتوح=hidden · مغلق=visible دائمًا | `shop.pages.js` | `filterA11y.lock` |
| 3 | التركيز لا يدخل اللوحة | كود desktop-dead | تركيز مؤجّل (60ms) على أول عنصر تفاعلي | التركيز على زر الإغلاق | `shop.pages.js` | `set()` |
| 4 | Tab يهرب | لا معالج | معالج `Tab/Shift+Tab` يحبس داخل اللوحة عند ≤768 | 20/20 داخل اللوحة | `shop.pages.js` | `onKeydown` |
| 5 | Escape لا يغلق | لا معالج | `Escape` يغلق + يوقف الانتشار | يغلق + تركيز يعود | `shop.pages.js` | `onKeydown` |
| 6 | التركيز على عنصر مخفي بعد الإغلاق | لا إرجاع | `set(false,true)` في ✕/تم/scrim + معلّق بعد إعادة الرسم | التركيز على زر الفلاتر | `shop.pages.js` | `set`, `afterRender` |
| 7 | `aria-expanded` عالق | تحديث جزئي | كل مسارات الإغلاق تمر عبر `set()` | متزامن دائمًا | `shop.pages.js` | `set()` |
| 8 | 5 عناصر قابلة للتبويب في لوحة مغلقة | transform فقط | `visibility: hidden` عند الإغلاق (≤768) | 0 عنصر | `shop.filters.css` | `.shop-filters__sheet` |
| 9 | حالة عالقة 768→769 | لا معالج resize | `onResize` ينظّف الحالة عند ≥769 | لا قفل/دور/حبس | `shop.pages.js` | `onResize` |
| 10 | تراكب فلاتر+بحث | لا إغلاق متبادل | إغلاق الآخرين عند الفتح + مستمع نقر على أزرار الهيدر | لا تراكب | `shop.pages.js` | `closeOthers`, listener |
| 11 | حلقة تركيز 2.83:1 (داكن) | لون أزرق داكن على سطح داكن | `--cat-focus` داكن = `--store-info` | 3.34–3.63 ✓ | `shop.css` | `#shop-view :focus-visible` |
| 12 | `aria-live` عام | سمة على الحاوية | إزالة + عدّاد `role=status` | إعلان مختصر دقيق | `index.html` + `shop.pages.js` | `.shop-toolbar__count` |
| 13 | هياكل بلا aria-hidden | لا سمات | `aria-hidden` للهياكل + `role=status` للرسالة | لا ضجيج + إعلان تحميل | `shop.js` | `loadingBlock` |
| 14 | فراغ/خطأ بلا دور + أيقونة تُقرأ | لا سمات | `role=status/alert` + أيقونة `aria-hidden` | إعلان صحيح | `shop.js` | `stateBlock` |
| 15 | زر إعادة المحاولة 42px | ارتفاع صغير | `min-height: 44px` | 44 | `shop.css` | `#shop-view .state .btn` |
| 16 | رابط المسار 42×21 | حجم النص | مساحة نقر `::after inset:-6px 0` | 42×33 بلا تغيير بصري | `shop.css` | `.shop-crumbs a` |

---

## 58. SUCCESS CRITERIA — الحالة

كل المعايير محققة: دور dialog ✓ · حالة الزر متزامنة ✓ · تركيز يدخل ✓ · حبس فقط في المودال ✓ · Escape يغلق ✓ · إرجاع التركيز ✓ · لا قفل عالق ✓ · تنظيف 768→769 ✓ · لا عناصر مخفية قابلة للتبويب ✓ · أسماء الحقول ✓ · checkbox أصلية ✓ · لوحة مفاتيح البطاقة ✓ · لا توقف «التفاصيل» ✓ · تباين التركيز ≥3:1 (داكن) ✓ · أهداف اللمس ✓ · تصنيفات/علامات ✓ · فراغ/خطأ ✓ · لا فائض جديد ✓ · 3C بلا تغيير ✓ · هندسة 3D/3E بلا تغيير ✓ · Home/Header/Menu/Search بلا تغيير ✓ · طلبات الشبكة بلا تغيير ✓ · Backend = 0 ✓ · `!important` جديد = 0 ✓ · console = 0 ✓.
**بند واحد معلّق لقرار منتج (لا يمنع الإغلاق تقنيًا):** §37 inStock معروض وغير فعّال.

---

## 59. FINAL REPORT — الخلاصة

- ملفات معدّلة: **5** (2 JS · 2 CSS · index.html لسمة واحدة) · Backend: **0** · مكتبات جديدة: **0** · `!important` جديد: **0**.
- 18 عيبًا مُدقّقًا (17 مُصلحة · 1 = قرار منتج) · 0 انحدارات · 0 فائض أفقي · 0 أخطاء console.
- القياسات كلها مولّدة آليًا: `audit3g-before/after.json` · `focus-sheet-before/after.json` · `matrix3g-after.json` · `extra3g-after.json` · `last3g-after.json` · `shots3g-after.json` + 42 لقطة.

**PHASE 3 READY TO CLOSE.**

> ملاحظة: البند §37 (inStock) هو **قرار منتج** أُبلغ عنه بلا إصلاح صامت؛ إن أردت معالجته فقرارك (إخفاء/تعطيل/إبقاء) لا يمنع إغلاق المرحلة تقنيًا.
> **PHASE 4 NOT STARTED** — بانتظار موافقتك.
